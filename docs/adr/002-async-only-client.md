# ADR-002: 라이브러리 API는 async-only로 둔다

- 상태: accepted — 규칙은 SKILL.md §4로 이관

> 이 결정은 핵심 구조가 아니라 순수 개발 규칙(코딩 컨벤션)이므로 본문 규칙은
> [`SKILL.md` §4](../../SKILL.md) #2로 이관했다. 분리 이전의 단일 파일 본문과
> 근거는 git history에 보존된다.
