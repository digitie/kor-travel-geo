# TASKS — 백로그

작업 항목은 `T-NNN` 형식의 ID로 관리한다. 새 작업은 "대기"의 우선순위 순서대로 들어가고, 진행 중이 되면 담당자를 표시한다. 완료된 작업은 "완료" 섹션 상단에 누적한다.

## 진행 중
- 없음.

## 대기 (우선순위 순)

> **작업 순서·번호 체계(ADR-050)**: ① 데이터 원천 보강·검증(**T-110~**) → ② 데이터 적재/백업 기능 구현·검증(**T-200~**) → (최하위 우선순위) v2 재audit(**T-105**) · v1 vworld 100% 호환(**T-106**). 원천 보강은 T-110부터 1씩, 적재/백업은 T-200부터 1씩 올리고, 중간에 추가되는 작업은 각각 T-1xx / T-2xx 번호로 채운다. T-105/T-106은 ID는 낮지만 우선순위는 **최하위**다. **T-109(이 PR #131)는 ②의 설계 문서이며, 구현은 아래 T-200대로 분할한다.** phase ① prototype은 `ops.source_*` registry 없이 로컬 디스크 경로로 독립 수행하고(역의존 금지), phase ②가 그 결과(C11~C17·go/no-go)를 DB case registry로 정식화한다. 단위별 상세 scope·의존성·근거는 ADR-050과 `docs/t109-backup-source-upload-management.md`의 구현 순서 절에 있다.

### 두 에이전트 병행 권장 순서

두 에이전트가 동시에 진행할 때는 **Agent A = Codex(데이터 원천 보강·검증 owner)**, **Agent B = Claude Code(source registry/upload/reconcile/match-set 기반 owner)** 로 나눈다. Agent B는 `T-206`을 `T-118` 확정 전에는 착수하지 않는다. `T-200`은 B 전 체인과 A의 `T-206` seed까지의 단일 root이므로 가장 먼저 머지해 모두의 대기를 푼다.

병행 운영 원칙 (**두 Agent가 병행하며 서로의 작업을 반드시 상호 리뷰한다** — 아래 PR·리뷰 루프를 두 Agent 모두 동일하게 따른다):

- **모든 Task는 PR 후 머지한다.** main 직접 commit 금지. 작업은 `agent/<a|b>-<...>` branch에서 하고 CI(backend/frontend/openapi) green 확인 후 squash 머지한다.
- **Task 착수 전(필수 선행) — 과거 모든 PR 리뷰 스캔:** 직전 PR만이 아니라 **과거 전체 PR(open/closed 무관, 자기 것·상대 것 모두)**을 검색해, 다른 Agent가 남긴 **아직 반영·답변하지 않은 리뷰 코멘트/리뷰 thread**가 있는지 확인한다. 있으면 그 리뷰를 **먼저 반영(또는 답변) → PR → 머지**한 뒤 새 Task를 시작한다. (한 번 처리한 리뷰는 답변/반영 코멘트로 종결해 다음 스캔에서 중복 처리하지 않는다.)
- **Task 머지 후 — 대기하지 않는다:** 자신의 Task PR을 머지한 뒤 상대 Agent의 리뷰를 **기다리지 않는다.** 리뷰가 없으면 그대로 다음 단계로 진행하고(필요하면 그대로 PR → 머지), 상대 리뷰는 위 "과거 모든 PR 리뷰 스캔"에서 **발견하는 즉시** 반영 → PR → 머지로 처리한다. (이전의 "리뷰가 달릴 때까지 대기" 규칙은 폐기.)
- **Task 머지 후(필수 후행) — 상대 PR 상세 리뷰:** 다음 Task로 넘어가기 전, **과거 전체 PR(open/closed 무관)** 중 다른 Agent가 올렸고 자신이 아직 리뷰하지 않은 PR에 **상세 리뷰 코멘트**(H/M/L 분류 + 잘된 점 + 머지조건/후속 권장)를 남긴다. 반영할 게 없으면 표준 종결 코멘트(예: "사후 리뷰 완료 — 블로킹 0 / 반영 불필요")로 닫는다.
- **작업 중 작업량이 늘어나면** 즉흥 확장하지 말고 **GitHub 이슈를 먼저 만들어** 범위를 적고 → 작업 → PR → 머지한다(이슈 → 작업 → PR → 머지).
- 작업 중에는 주기적으로 `origin/main`과 현재 작업 branch의 최신 상태를 확인하고, 장기 작업은 중간에 rebase 또는 fast-forward로 충돌을 작게 유지한다.
- Git 작업은 Windows Git과 NTFS worktree에서 수행하고, 그 외 의존성 설치·테스트·장기 실행 검증은 WSL ext4 테스트 미러에서 수행한다.
- 작업 중 반복적으로 문제가 되는 패턴은 `docs/agent-failure-patterns.md` 또는 관련 workflow 문서에 기록해 같은 실패가 반복되지 않게 한다.

1. 1차 병행
   - Agent A: `T-110` → `T-111` → `T-112` → `T-113` → `T-114` → `T-115` → `T-116` → `T-117` → `T-118`. 의존 없는 `T-120`(epost 우편번호 수동 적재·검증; `T-207`과 **공유 검증 모듈**)도 이 구간에 병행한다 — `T-207`이 `T-120`에 의존하므로 2차 전에 끝나 있어야 한다.
   - Agent B: `T-200` → (`T-201` ∥ `T-202`; 둘 다 `T-200`만 의존) → `T-203`(`T-201`·`T-202` 모두 필요) → `T-204` → `T-205`
2. 2차 합류 (`T-118` 확정 후)
   - Agent A: `T-206`의 C11~C17 registry seed·metric mapping(B 뼈대 머지 후) → `T-207` 주도(epost 적재/검증 로직; upload/RustFS 플럼빙은 B 제공)
   - Agent B: `T-206` backend registry/API/run-validation 뼈대 → `T-208` → `T-209`. `T-207`·`T-208`은 `T-206`에 의존하지 않으므로 `T-206`과 병렬 진행 가능하다.
3. 최종 검증·운영 보강
   - Agent A+B: `T-210`
   - Agent B: `T-211` → `T-212`
   - Agent A: `T-121` → `T-122` → `T-123` (phase ① 전국 라이브·벤치·최종 검증; `T-211`·`T-212`와 병렬)
   - Agent A(Codex): `T-214` 완료. T-213 전용 baseline `kor_travel_geo_t213_20260615_r3` / `kor-travel-geo/t213/20260615-rerun3` / `F:\dev\geodata\t213-baseline\20260615-rerun3\`를 preflight로 확인하고 benchmark artifact를 `F:\dev\geodata\t214-benchmark\20260615-r3\`에 보존했다.
   - Agent A+B: `T-215` 완료. T-215에서 분리한 REST c64 tail과 C11~C17 optional source run-validation은 T-216/T-126 live acceptance로 완료했다.
   - Agent A: `T-119`는 T-125 후속 검증(`T-129`~`T-134`·`T-137`)·Admin UI 반영(`T-220`/`T-221`)과 ADR-051 승인 시에만 별도 진행하고, 승인되지 않으면 검증 전용 결론을 유지한다.
   - Agent A+B: `T-138`에서 적재 후 read-mostly serving 경로를 benchmark/index/MV/API 계약까지 포함해 튜닝한다. 그래도 목표 속도에 못 미치면 `T-139`에서 별도 변경 DB를 만들어 현 DB와 구조 변경안을 비교한다.
4. 신규 안정화·성능·Admin UI·백업 병행 (**번호 규칙: T-1xx = 성능/기능/geocoder/안정성, T-2xx = Admin UI + 데이터 적재/백업/복원**)
   - Agent A(Codex): `T-129` → `T-130` → `T-131` → `T-132` → `T-133` → `T-134` → `T-137`로 C11 후속 gate를 닫는다. 이와 병렬로 `T-138`·`T-140`~`T-146`과 신규 성능/안정성/관측성 `T-154`~`T-164`, geocoder/reverse 정확도 `T-165`~`T-176`을 진행해 정확도·성능·고부하 안정성을 끌어올린다. `T-139`는 T-138/T-146 이후 목표 미달일 때만 별도 변경 DB 실험으로 착수한다. (T-166~T-168 국가지점번호 forward 게이트 분리·reverse 코드 방출은 `docs/v2-cleanslate-optional-data-accuracy.md` 권고를 코드화하는 저비용·고확실 항목이라 우선 배치 가능.)
   - Agent B(Claude Code): Admin UI는 전부 T-2xx로 옮겼다 — `T-220` → `T-221`로 실제 활용 파일과 Admin UI 상태 표시를 맞추고(과거 T-135/T-136), `T-222`~`T-227`로 기능·편의성·Playwright e2e를 보강한다(과거 T-147~T-152). 신규 백업/복원 Admin UI·e2e(`T-248`~`T-258`)와 source-files 단계별 e2e(`T-259`~`T-263`)도 B가 맡는다. T-138/T-144/T-169에서 API 계약·v2 enum을 바꾸면 Agent B가 UI typegen/e2e 반영을 병행한다.
   - Agent A+B(백엔드 백업/복원): `T-228`~`T-247`의 백업/복원 기능·검증·hot-swap·통합 테스트·benchmark는 Claude가 도메인을 주도하고(B), `pg_terminate_backend`·restore-drill 등 부하·DB 작업은 Codex(A)와 시간대 조율한다(A+B).
   - Agent A+B: `T-153`에서 두 트랙(A 성능·정확도 `T-138`/`T-140`~`T-146`/`T-154`~`T-176`, B Admin UI·백업/복원 `T-220`~`T-263`)의 결과를 같은 benchmark/e2e/문서 gate로 묶어 최종 안정화 acceptance를 수행한다.

### ① 데이터 원천 보강·C11 후속 검증 (T-110~, phase 1 · T-1xx)

- T-132 C11 guarded 후보 검증 harness 확장 — T-131 정책을 반복 검증할 수 있도록 `scripts/run_t125_c11_serving_preflight.py`를 policy mode/threshold flag/sample export/결정적 summary schema/source detail column/cleanup 검증까지 확장하거나 후속 script를 추가한다. active serving object는 변경하지 않으며, 단위 테스트는 fixture 기반으로 정책 분기와 artifact schema를 고정한다. (의존: T-125, T-131과 병렬 가능)
- T-133 C11 shadow serving 성능·rollback 리허설 — T-131/T-132에서 통과한 정책이 있을 때만 shadow table/MV 또는 read-only candidate path로 SQL/REST p95를 T-214/T-216/T-217 baseline과 비교한다. flag off에서는 기존 `mv_geocode_target` row/hash/대표 표본이 동일해야 하고, flag on에서는 SQL/REST p95 회귀 5% 이하, 오류 0건, rollback(`flag off + refresh/drop shadow` 또는 동등 hot-swap) 절차를 artifact로 증명한다. active serving promotion은 하지 않는다. (의존: T-131, T-132)
- T-134 C11 v1/v2 노출 계약 및 `pt_source`/`coord_source_detail` 결정 — v1 VWorld 호환 표면에서는 `pt_source`를 기존 `entrance`/`centroid` 값으로 유지할지, 세부 출처를 `x_extension.coord_source_detail`로만 낼지, v2 전용 필드로 분리할지 결정한다. OpenAPI/UI 타입 영향, T-105 v2 재audit, T-219 v1 계약 정합 후속과 충돌하지 않게 문서와 테스트 계획을 만든다. 코드 변경은 계약 확정 뒤 별도 PR로 제한한다. (의존: T-125, T-128)
- T-137 T-125 후속 종합 gate 및 ADR-051 재판정 — T-129~T-134 + Admin UI 반영(T-220/T-221) 산출물을 묶어 C11을 계속 no-go로 둘지, validation-only로 고정할지, guarded policy로만 조건부 go를 검토할지 최종 판정한다. `docs/t125-c11-serving-preflight-result.md`, `docs/t118-phase1-go-no-go.md`, ADR-051, `docs/resume.md`, 이 백로그를 갱신하고, gate가 통과하더라도 T-119 착수는 사용자 승인 전까지 금지한다. (의존: T-129~T-134, T-220, T-221)
> Admin UI 정합 감사(과거 T-135)와 T-125 적재 변경점 Admin UI 반영(과거 T-136)은 사용자 번호 규칙(Admin UI=T-2xx)에 맞춰 **T-220/T-221**로 이동했다 — 아래 ②의 "Admin UI(T-2xx)" 절 참조.

### 성능·안정성·고성능 geocoder/reverse 강화 (T-138~ · T-1xx · 주 Agent A, 계약/typegen 변경 시 A+B)

> 역할 경계(중복 방지): **T-138** = index/MV/EXPLAIN 중심 read-mostly 튜닝(결과캐시·prepared·풀은 T-156/T-155/T-154로 위임). **T-141** = 고부하 측정 matrix(합격 budget/누수·회귀 gate는 T-163/T-164). **T-145** = per-endpoint concurrency cap·circuit breaker·retry 금지 우산(pool/cancel/readiness 세부는 T-154/T-161/T-160). **T-146** = maintenance pipeline/DDL/runbook(재기동·swap 후 runtime warm은 T-162). **T-140 corpus**는 변형/경계/negative/confidence-golden 필드를 수용하도록 확장해 T-165/T-171/T-172/T-176 case를 등록만 한다.

- T-138 read-mostly serving 속도 튜닝·벤치마크·Index/MV/API 최적화 — 적재 후에는 write가 사실상 없고 read가 대부분이라는 운영 전제를 명시적으로 활용해 T-213 r3/T-214/T-216/T-217 baseline을 재측정하고, geocode/reverse/search/zipcode/admin 조회 경로의 p50/p95/p99/tail을 줄인다. 아직 배포 전이므로 호환성 유지나 코드 수정 최소화보다 성능·안정성을 우선한다. `pg_stat_statements`, `EXPLAIN (ANALYZE, BUFFERS)`, index usage, cache warm/cold, REST pool/admission 영향을 함께 본다. 후보는 covering/partial/functional/trigram/GiST index, denormalized materialized view, query별 slim MV, precomputed search key, post-load `ANALYZE`/`VACUUM`/statistics target, refresh/swap runbook뿐 아니라 API 계약 변경도 포함한다. 예: payload 축소, geometry 기본값 변경, endpoint/query parameter 재설계, pagination/limit 정책 변경, admin summary endpoint 분리, v2 response field 재배치가 성능·안정성에 유리하면 제안·실험한다. 계약을 바꾸는 경우 OpenAPI/typegen/UI/문서/회귀 테스트와 migration note를 같은 PR 범위에 포함하고, 기존 ADR과 충돌하면 ADR 갱신안을 같이 낸다. 산출물은 `docs/t138-read-heavy-serving-performance.md`, benchmark artifact, 후보별 DDL/API diff/rollback plan이며, 목표 속도에 못 미치면 구조 변경 후보를 T-139로 넘긴다. (의존: T-214, T-216, T-217)
- T-139 DB 구조 변경 실험 DB 비교 및 최적안 정리 — T-138의 index/MV/API 튜닝으로 충분히 빠르지 않을 때만 진행한다. 현 DB(`kor_travel_geo_t213_20260615_r3`)와 같은 원천/같은 commit/같은 benchmark corpus로 별도 변경 DB를 만들고, 더 과격한 구조 변경안을 실험한다. 후보는 read 전용 projection table, `mv_geocode_target`/`mv_geocode_text_search` 재분해, 주소 검색 전용 pre-tokenized table, geometry/point 분리, partitioning/clustered layout, key별 denormalized lookup table, 대형 polygon 검증 table과 serving table 분리, API 응답에 맞춘 pre-shaped serving table 등이다. 각 변경 DB는 load/rebuild/MV refresh 시간, storage/index size, backup/restore size, SQL/REST latency, consistency count, API 응답 hash 또는 새 계약의 golden response, 운영 rollback 난이도를 현 DB와 같은 표로 비교한다. 최종 산출물은 `docs/t139-db-structure-comparison.md`와 "현 구조 유지 / additive 튜닝만 적용 / API 계약 변경 포함 / 구조 변경 PR 분리" 중 하나의 권고안이며, 실제 migration은 별도 후속 Task로 다시 쪼갠다. (의존: T-138 목표 미달)
- T-140 geocoder/reverse-geocoder golden corpus 확장 — 정확도와 회귀 방지를 위해 전국 대표 주소 corpus를 새로 만든다. 범위는 도로명 exact/fuzzy, 지번, 건물명, 시군구용건물명, 단독 행정구역, 우편번호, 사서함/다량배달처, 국가지점번호, 복합 시군구 suffix, 산/본번/부번, 동명이인 도로명, 좌표 reverse 경계점, polygon boundary/near-boundary, 바다/산악/도서 지역, 잘못된 좌표/주소 negative case를 포함한다. 각 case는 기대 후보, 허용 거리, 응답 hash/golden field, 성능 budget, 원천 근거를 가진다. 산출물은 `docs/t140-geocoder-golden-corpus.md`, corpus 파일, DB fixture/live mode runner다. (Agent A, 의존: T-125, T-214)
- T-141 SQL/REST 고부하 benchmark matrix 확장 — T-047/T-214/T-216/T-217 benchmark를 고부하 운영형 matrix로 확장한다. workload는 geocode exact/fuzzy/search/reverse/zipcode/admin summary를 실제 비율 mix, worst-case mix, adversarial fuzzy mix, reverse polygon-heavy mix로 나누고, concurrency 1/4/16/64/128/256, warm/cold cache, pool 크기, statement timeout, admission limit, 30~60분 soak, burst/recovery를 측정한다. 산출물은 p50/p95/p99/max/error/timeout/cancel/queue wait/DB execute/pool checkout/CPU/RSS/IO/pg_stat_statements delta를 포함한다. 목표는 고부하에서도 오류 0 또는 명시 error budget, p99 tail 안정성, 반복 run 간 편차를 확인하는 것이다. (Agent A, 의존: T-138)
- T-142 reverse-geocoder 공간 조회 최적화 — reverse path의 행정구역 polygon, 건물 polygon, 우편번호 polygon, 국가지점번호, nearest-road/building 후보 조회를 분리 측정하고 최적화한다. 후보는 `ST_Subdivide`/parts table, KNN GiST, bbox prefilter, point-to-region ancestry precompute, geometry simplification 허용 여부, `ST_Covers`/`ST_Intersects` 선택, reverse 전용 MV/table, 좌표 grid bucket이다. boundary correctness golden case와 성능 benchmark를 동시에 통과해야 하며, 좌표 외부 인터페이스는 `(lon, lat)` 유지한다. (Agent A, 의존: T-140, T-141)
- T-143 geocode/search query plan 안정화 — exact/fuzzy/search path를 입력 유형별로 분해해 planner 변동과 tail latency를 줄인다. 후보는 exact preflight 재정렬, normalized/generated column, trigram index/threshold `SET LOCAL`, token table, prefix/suffix index, region hint 물리 컬럼, `LIMIT` pushdown, score 계산 단계 분리, broad fuzzy fallback 차단/축소, 통계 target 조정이다. query plan은 corpus별 `EXPLAIN (ANALYZE, BUFFERS)`로 고정하고, 입력 분포가 바뀌어도 p99가 튀지 않는지 반복 측정한다. (Agent A, 의존: T-140, T-141)
- T-144 성능 우선 v2/API 계약 재설계 후보 검증 — 아직 배포 전이라는 전제를 반영해 성능·안정성에 유리한 API 계약 변경을 적극 검토한다. 후보는 `include_geometry=false` 기본화 또는 geometry endpoint 분리, 응답 field slim mode, detail expansion endpoint, reverse/geocode/search 분리 응답 schema, pagination/limit hard cap, admin summary 전용 endpoint, streaming/progress endpoint 분리, pre-shaped response table에 맞춘 DTO 재배치다. 각 후보는 OpenAPI/typegen/UI 수정량보다 latency, payload size, 오류율, 구현 복잡도, 안정성 이득을 우선 평가한다. 선택된 변경은 ADR, migration note, golden response test, UI 반영 계획을 포함한다. (Agent A+B, 의존: T-138, T-140)
- T-145 운영 안정성·backpressure·fail-fast 보강 — 고부하에서 API가 느리게 망가지는 대신 예측 가능하게 제한되도록 admission control, pool timeout, statement timeout, cancellation, retry 금지 구간, slow query sampling, circuit breaker, per-endpoint concurrency cap, readiness degradation, overload error envelope를 점검한다. SQL/REST benchmark와 soak test에서 timeout storm, pool starvation, memory growth, stalled SSE, worker restart 후 회복을 검증한다. (Agent A, 의존: T-141)
- T-146 post-load read-optimized maintenance pipeline — 적재 완료 후 write가 거의 없다는 전제로 read 최적화 maintenance를 표준화한다. `ANALYZE`, `VACUUM (ANALYZE)`, 필요 시 `REINDEX CONCURRENTLY`, `CLUSTER` 또는 물리 정렬 대안, MV refresh/swap, statistics snapshot, bloat check, index size budget, optional `pg_prewarm`/cache warming runbook을 정리한다. rebuild 시간, API downtime 영향, storage 증가, rollback 가능성을 함께 측정하고 자동화 후보와 수동 runbook을 분리한다. (Agent A+B, 의존: T-138)
- T-154 async DB pool 포화·checkout timeout 튜닝 + 풀 포화 fail-fast — `engine.py`에 pool_size/max_overflow/pool_timeout/pre-ping을 명시 설정하고, 풀 고갈 시 무한 대기 대신 명시적 503/E0409 fail-fast로 전환한다. T-047 c64 tail의 주원인인 checkout 대기를 계측·상한한다. 합격: 풀 포화 부하에서 무한대기 0·명시 에러·p99 안정, 오류 0. (Agent A, 의존: T-138, T-141)
- T-155 psycopg server-side prepared statement·plan cache 튜닝 — hot 쿼리(geocode/reverse/search)의 `prepare_threshold`·서버 prepared statement 재사용을 측정·튜닝하고 plan 캐시 효과와 pool 재사용 상호작용을 확인한다. 합격: 동일 쿼리 반복 시 plan 재사용 증가·p95 개선·결과 회귀 0. (Agent A, 의존: T-138)
- T-156 geocode/reverse hot-key 결과 캐시 전략·무효화 — 현재 `geo_cache` 테이블/인덱스/metric은 있으나 라우터에 미연결(gap)이다. hot-key 결과 캐시(in-proc LRU 또는 geo_cache)와 적재/MV swap 시 무효화 규약을 설계·측정한다. 합격: hot-key hit율·p95 개선, swap 후 stale 응답 0. (Agent A, 의존: T-138)
- T-157 pg_stat_statements 상시 수집·persist·Admin/Prometheus 노출 — 상시 수집 + 주기 스냅샷 persist, top-N slow query를 Prometheus/Admin에 노출한다. 합격: 재기동 후에도 누적 통계 유지·top-N 노출. (Agent A, 의존: —)
- T-158 slow-query·overload 구조화 로깅·sampling persist — 임계 초과 쿼리/요청을 구조화 로깅 + sampling으로 persist(과다 로깅 방지)한다. 합격: 느린 쿼리 표본이 endpoint/plan과 함께 남고 정상부하 로깅 비용은 무시가능. (Agent A, 의존: T-157)
- T-159 DB 단절·복구·IO 지연 chaos/fault injection 하 graceful degradation — DB 끊김/복구/IO 지연을 주입해 API가 graceful degrade(fast-fail·자동 회복)되는지 검증한다. 합격: 단절 중 fast-fail·복구 후 자동 회복·crash 0. (Agent A, 의존: T-160)
- T-160 DB 의존 readiness/health 엔드포인트·degradation 신호 — readiness가 DB/pool 상태를 반영하고 과부하 시 degradation 신호를 노출한다. 합격: DB 불가 시 ready=false·degraded 신호 정확. (Agent A, 의존: —)
- T-161 client disconnect·query cancellation 일관성·cancel 관측 — 클라이언트 disconnect 시 진행 쿼리 cancel 일관성·누수 없음·관측 지표를 보강한다. 합격: disconnect 후 orphan 쿼리/커넥션 0·cancel 카운터 노출. (Agent A, 의존: T-141)
- T-162 cold-start·cache warm 자동화·재기동 후 일관 성능 — 재기동/swap 후 cache warm(`pg_prewarm` 등)을 자동화해 cold p99 spike를 완화한다(T-146 maintenance와 경계: 여기는 runtime warm 코드). 합격: 재기동 직후 p99가 warm 대비 일정 배수 이내. (Agent A+B, 의존: T-138)
- T-163 60분 soak 메모리/IO/CPU budget·누수 감지 가드 — 장시간 soak에서 RSS/IO/CPU budget·누수 판정식을 정해 회귀 가드한다(T-141 측정 위에 합격 기준). 합격: 60분 soak RSS 증가 budget 이내·누수 0·오류 0. (Agent A, 의존: T-141)
- T-164 adversarial/입력분포 변화 하 p99 안정성 회귀 가드 CI화 — 입력 분포(adversarial fuzzy 등)가 바뀌어도 p99가 튀지 않는지 회귀 gate를 CI/nightly에 고정한다. 합격: 분포 변형 set에서 p99 회귀 임계 초과 시 fail. (Agent A, 의존: T-141, T-163)
- T-165 주소 정규화/파싱 견고성 강화 — 이형 표기·약어·공백·괄호·구주소/신주소·영문혼용·오타 케이스 견고성을 강화하고 변형/경계 case를 T-140 corpus에 등록한다. 합격: 변형 corpus 통과율 목표 달성·기존 회귀 0. (Agent A+B, 의존: T-140)
- T-166 국가지점번호 forward를 makarea 게이트에서 분리 — 좌표는 항상 계산값(10m 셀)을 반환하도록 `core/geocoder.py:91-96`의 makarea None→NOT_FOUND를 제거하고 makarea는 enrich로 강등한다. **신규 데이터 0**(clean-slate 분석 권고 1). 합격: 의무존 밖 유효 코드도 좌표 반환·기존 동작 회귀 0. (Agent A, 의존: —)
- T-167 국가지점번호 한국 envelope/grid bound-check 추가 — `core/sppn.py:88-99`의 헐거운 bound를 강화해 바다·국경 밖 좌표 산출을 차단한다. 합격: 한국 envelope 밖 코드 거절·정상 코드 회귀 0. (Agent A, 의존: T-166)
- T-168 reverse-geocode에서 국가지점번호 코드 first-class 방출 — `format_national_point_number_from_5179`를 reverse 경로에 배선해 무주소 좌표→코드를 반환한다(순수 계산). 합격: reverse가 무주소 좌표에 국가지점번호를 반환·기존 reverse 회귀 0. (Agent A, 의존: T-166)
- T-169 v2 match_kind/point_precision enum 정직화 — `dto/v2.py`에 sppn `grid_cell`·`detail`·`poi`를 추가하고 죽은 값(미사용 postal/category·cache 등)을 정리한다. T-134(C11 pt_source 계약)와 v2 enum 변경 PR/순서를 ADR로 조율한다. 합격: enum이 실제 producer와 일치·typegen drift 0. (Agent A+B, 의존: T-134 조율)
- T-170 v2 producer 1:N candidate-list 전환 — `core/v2.py`의 단일 candidate collapse를 해제해 다점 후보를 방출한다(wire `candidates` tuple 무변경). 합격: 다점 후보 dedup 방출·v1 무영향. (Agent A+B, 의존: T-169)
- T-171 fuzzy ranking 결정성·품질 보강 — tie-break·threshold 일관성·동률 정렬 결정성을 보강한다(case는 T-140 corpus 등록). 합격: 동일 입력 결과 결정적·corpus ranking 품질 목표. (Agent A, 의존: T-140)
- T-172 confidence 산정 결정성·교정 중앙 모델 — centroid 캡·sppn 하드코딩 등을 중앙 confidence 모델로 일관화한다. 합격: confidence가 거리/매치종류와 단조·golden 고정. (Agent A, 의존: T-140)
- T-173 negative/악성/경계 입력 안전성 하니스 — geocode/reverse/sppn에 악성·경계 입력 안전성(예외→구조화 4xx)을 하니스로 고정한다. 합격: 악성 입력 set 전부 구조화 응답·500/크래시 0. (Agent A, 의존: T-140)
- T-174 좌표계(EPSG:5179↔4326) 왕복 정밀도 검증·변환 경로 통일 — 왕복 정밀도를 검증하고 변환 helper 경로를 단일화한다. 합격: 왕복 오차 임계 이내·변환 helper 단일. (Agent A, 의존: —)
- T-175 region hint 정확도·교차검증 — `sig_cd`/`bjd_cd` 필터 정합성을 교차검증한다. 합격: hint 적용/미적용 결과 정합·오적용 0. (Agent A, 의존: T-140)
- T-176 reverse 경계·근접 정확도 정합 — both fan-out·동률 거리·radius 경계 규약을 고정한다. 합격: 경계 case 결정적·radius 포함/제외 명시·먼 좌표 NOT_FOUND/OK 의미 일관. (Agent A, 의존: T-140)

### 최종 안정화 acceptance (A+B)

- T-153 geocoder/Admin UI/백업 최종 안정화 acceptance (**번호 규칙 예외**: T-1xx·T-2xx 두 트랙을 묶는 통합 capstone이라 어느 한 범위에도 속하지 않는 의도된 예외 ID다 — 새 acceptance task는 트랙별로 T-1xx/T-2xx에 만든다) — Agent A 성능·정확도 트랙(T-138·T-140~T-146·T-154~T-176)과 Agent B Admin UI·백업/복원 트랙(T-220~T-263)을 하나의 최종 gate로 묶는다. 산출물은 golden corpus 통과율, C1~C17 상태, SQL/REST p95/p99/error budget, soak 안정성, API 계약 변경 목록, Admin UI Playwright 결과, 백업/복원 round-trip·restore-drill 결과, React Doctor, OpenAPI/typegen drift, 운영 runbook 갱신 여부를 한 문서에 정리한다. gate 미달 항목은 release blocker와 후속 Task로 분리한다. (Agent A+B, 의존: T-140~T-176, T-220~T-263)
- T-127 optional source 구조 validator 강화 — T-216에서 `warning`으로 통과한 optional 검증 원천의 상세 profile을 보강한다. 대상은 `detail_address_db_full`, `national_point_grid_shape`, `national_point_grid_center`, `civil_service_institution_map`, `address_db_full`, `building_db_full` 등 single-file optional category이며, archive member naming/필수 파일/sidecar/기준월 sanity를 category별로 검증해 `warning`과 `failed`의 의미를 더 좁힌다. C11~C17 run-validation 수용 결과는 유지하고, 새 validator는 별도 fixture와 실제 원천 smoke로 고정한다. (의존: T-216)

### ② 데이터 적재/백업/복원 + Admin UI (T-200~ · T-2xx · phase 2 — T-109 설계 구현)

> 개인 서버·낮은 HW 신뢰성 전제 → **백업/복원이 간단하고 안정적인 것**이 최우선. 아직 배포 전이므로 호환성·코드수정 최소화보다 성능·안정성을 우선한다. backup/restore 백엔드는 Claude(B)가 도메인 주도, `pg_terminate_backend`·restore-drill 등 DB 부하는 Codex(A)와 조율(A+B). Admin UI/e2e는 전부 B. 백엔드 기존 자산: `infra/backup.py`, `/v1/admin/backups|restores`, `dto/admin.py`, ADR-030(parallel dump+압축)/ADR-036(`ALTER DATABASE RENAME` hot-swap), `docs/t046`·`t058`·`backup-restore-source-inventory.md`. (시퀀싱: expires_at→janitor→스케줄; verify→카탈로그→manifest UI; dry-run→version guard→복원 위저드; row_count/reconcile→restore-test; round-trip→장애주입; hot-swap 실행→hot-swap 통합. UI 탭 셸이 root, e2e는 구현 머지 후.)

- T-210 통합/성능 검증 — 백엔드 fixture 통합 27 시나리오(#27 epost 포함) + `KTG_SLOW_REAL_DATA` 실데이터, 백엔드 `openapi.json` drift 0 + 통합 후 `api.gen.ts` 재생성 drift 0(T-209가 갱신한 api.gen.ts 기준이라 T-209 머지 후 검증; UI e2e/프론트 단위 테스트 자체는 T-209 소유), 장비 비종속 성능(fixture 기반 deep rehash·multipart 대용량 회귀·메모리 상한)은 여기서 수행하고 실측 throughput만 N150/Odroid(T-063)로 연계. **[부분 완료, Agent B]** 백엔드 통합 18 시나리오(라이브 PostGIS + FakeS3Store harness) + 런타임 버그 2건 수정을 #162로 머지. #27 epost 시나리오는 T-207 unit/API fake 테스트로 위임(통합 suite는 외부 epost OpenAPI를 호출하지 않음). T-209 머지 후 `openapi.json`/`api.gen.ts` drift 0 검증 완료. 장비 비종속 fixture 성능(deep rehash `sha256_file`·multipart 대용량 `_read_file_chunks`·메모리 상한)은 `scripts/benchmark_source_registry_perf.py` + 상시 tracemalloc 가드(`tests/unit/test_t210_source_registry_perf.py`)로 구현(`docs/t210-device-independent-perf.md`). **잔여**(rebuild-db SHP/text 로더 전국 적재 통합·`KTG_SLOW_REAL_DATA` 실데이터)는 GDAL·전국 데이터 필요한 A+B 공동이며 T-213(#172) 결과와 합친다; 실측 throughput은 T-063. (의존: T-205·T-206·T-207·T-208·T-209)
#### Admin UI (T-2xx · 주 Agent B) — 과거 T-1xx에서 이동

- T-222 성능·검증 artifact Admin UI 노출 (과거 T-147) — T-138/T-141/T-146 benchmark artifact, C1~C17 validation result, source match set 상태를 read-only summary로 비교 노출(최근 run·baseline 대비 증감·p95/p99·실패 case·artifact 링크)한다. **backup/restore artifact는 백업 트랙(T-252/T-253)이 담당**하고 여기서는 성능·C1~C17 검증 artifact에 한정한다. (Agent B, 의존: T-138, T-141)
- T-225 source-files 검증·매칭·DB 입력 fixture harness (과거 T-150) — e2e가 백엔드 live에만 의존하지 않도록 deterministic fake API/fixture(upload session state machine, validation result, match set, active/current config, job events, SSE disconnect/reconnect, error)를 만든다. **T-259~T-263 단계별 e2e에 fixture를 공급**한다. (Agent B, 의존: T-223)
- T-226 Admin UI 운영 편의 기능 보강 (과거 T-151) — 현재 구성 diff, match set 비교, 검증 case drilldown, DB 입력 전 preflight checklist, artifact 링크, 작업 재개 목록, 위험작업 typed confirmation preview, role/permission 안내, 빈/부분 실패 상태를 보강한다. 대형 목록 가상화는 T-254 공통 컴포넌트를 공유하고, 느린 네트워크에서 버튼 중복 실행을 막는다. (Agent B, 의존: T-222, T-224)
- T-227 Admin UI 접근성·성능·회복성 e2e (과거 T-152) — `/admin/source-files`·`/admin/consistency`·`/admin/ops`의 keyboard/focus trap/dialog/skeleton/error boundary/route/large list/slow API/SSE 끊김/refresh 복원을 Playwright+React Doctor로 검증한다(**기존 표면 한정**; 신규 backups 위저드/hot-swap dialog는 T-258). 목표는 장시간 운영 콘솔에서 메모리·렌더·상태 sync 안정성. (Agent B, 의존: T-223, T-226)

#### 백업/복원 기능·검증 (T-228~ · Claude 주도, DB 부하는 Codex 조율 = A+B)

- T-237 일관 스냅샷 백업 profile — pg_dump만으로는 재현성 부족(`backup-restore-source-inventory.md`). dump 직전 active match set RustFS object를 HEAD로 확인해 `source_inventory_verification`(ok/missing/mismatch)을 manifest에 기록하고, active serving_release/dataset_snapshot/match_set id를 manifest 상단에 요약한다(secret 원문 금지). RustFS 비활성 시 백업은 성공하되 skipped 기록. 합격: 인벤토리 요약·release 요약 포함·secret 미포함 가드. (A+B, 의존: —)
- T-238 백업 manifest↔DB↔RustFS 3자 정합 reconcile(opt-in) — manifest `source_match_set` per-file을 RustFS HEAD로 대조해 present/missing/etag_mismatch 분류(`verify_restore_source` 경로 재사용). RustFS 비활성/legacy manifest graceful. live/RustFS gate marker로 기본 CI skip. 합격: object 1건 누락 모사 시 정확 보고. (Agent A, 의존: T-237, T-244)
- T-239 스케줄 백업 due-check 트리거 — 수동 백업만 있어 사람이 잊으면 끊긴다. 외부 cron이 호출하는 멱등 `POST /backups/scheduled/run-due`(interval 미경과 시 no-op, lock으로 중복 enqueue 방지) + `status`(last/next_due/keep_min), 스케줄 백업은 retention_class=scheduled로 janitor keep_min 존중. 합격: 멱등·정확히 1건 enqueue·동시호출 중복 0. (A+B, 의존: T-229, T-230)
- T-240 백업 카탈로그 조회 고도화 — GET /backups 응답에 expires_at·retention_class·verification 상태·source_set 요약·copy_count를 manifest에서 추출 노출하고 `expiring_within_days`/`verified` 필터를 추가(read-only, manifest 누락 시 None). 합격: 필드/필터 동작·OpenAPI drift 0. (A+B, 의존: T-229, T-231)
- T-241 복원 hot-swap 실행 endpoint — ADR-036/T-058은 plan/preflight까지만 구현(`t058:217`). active maintenance window + typed confirmation으로 `ALTER DATABASE RENAME` 2-step(current↔restore)을 실행하고 engine pool refresh·post-swap smoke·실패 시 자동 rollback·`serving_releases`(previous_release_id)+audit 4종(started/succeeded/failed/rolled_back)을 기록한다. major mismatch hard-fail, 동시 hot-swap advisory lock 제한. 합격: 정확 confirmation일 때만 rename·smoke 실패 시 자동 rollback·동시 두번째 fail-fast. (A+B, 의존: —)
- T-242 restore-test/restore-drill 자동화 + runbook — '복원 가능한가'는 복원을 해봐야 안다. 임시 DB(`..._restoretest_<ts>`)에 new_database 복원→reconcile/smoke→임시 DB drop을 1회 명령(`ktgctl backup restore-drill`)으로 완주하고 결과 artifact(PASS/FAIL·시간·크기)를 남긴다. 운영 serving DB·현재 DSN과 충돌 금지(가드). `docs/restore-drill-runbook.md`(주기·디스크 점검·실패 알림). 합격: 성공/실패 모두 임시 DB·work_dir 정리·reconcile FAIL 시 비0 exit. (A+B, 의존: T-233, T-235)
- T-243 부분 손상 아카이브 best-effort 복구 — `verify_internal_checksums`는 하나만 깨져도 전체 중단. `allow_partial=true`일 때만 손상 파일 목록을 수집하고 `pg_restore --use-list`로 멀쩡한 테이블만 복원, 복구/스킵 목록을 `partial_restore` 블록에 기록(`toc.dat`/`manifest.json` 손상은 명확 실패). 기본 비활성(회귀 0), 비상 최후 수단임을 문서화. 합격: 일부 .dat 손상 시 해당 테이블만 스킵·allow_partial=false 시 기존 동작. (A+B, 의존: —)
- T-244 backup→restore round-trip live 통합 테스트(opt-in marker) — `run_backup_job`/`run_restore_job` 전체 흐름 통합 테스트가 없다. 소형(대구 부분) DB로 backup→`.tar.zst`/manifest→new_database restore→pg_restore/analyze/smoke→원본 vs 복원 ROW_COUNT_OBJECTS 10개 일치를 `@live` marker로 검증(도구 미설치 시 skip). 손상·중단 통합 테스트의 fixture 토대. 합격: marker on 통과·off skip·CI green 유지. (A+B, 의존: —)
- T-245 복원 장애 주입 live 통합 테스트 — round-trip fixture를 재사용해 손상 아카이브(sha256 flip)·truncated tar·내부 checksum 위조·checksum 누락 4케이스가 모두 복원 실패+target 미적재, 백업 중단/cancel 시 artifact=failed·최종 archive 미존재·`.part`/work_dir 삭제, replace_current confirmation/target_dsn/window 가드를 검증한다. 합격: 전 케이스 안전 거절·live off skip. (Agent A, 의존: T-244)
- T-246 hot-swap·rollback round-trip live 통합 테스트 — `t058:258` 후속. 소형 DB로 backup→restore→hot-swap→smoke→rollback round-trip + release lineage(previous/rollback_target)·audit 4종·retention 종료 후 rollback 거절·동시 hot-swap fail-fast를 `@integration`(Docker PostGIS 15434)로 검증. 합격: round-trip 통과·rollback 후 lineage 보존·비통합 CI skip. (A+B, 의존: T-233, T-241)
- T-247 backup/restore 소요시간·archive 크기 benchmark 스크립트 — `scripts/benchmark_backup_restore.py`로 profile×jobs(1/2/4)×compression(3/9/19) matrix의 backup/restore wall-clock·dump/archive 크기·압축률을 측정(기존 benchmark_*.py JSON 형식)하고 저사양(N150/Odroid) jobs/compression 트레이드오프를 summary로 정리한다. `t055` 3절·T-063 입력. 합격: matrix 측정·JSON 일관·PLAN_ONLY 확인. (Agent A, 의존: T-244)

#### 백업/복원 Admin UI + e2e (T-248~ · Agent B)

- T-248 `/admin/backups` 탭 셸 + 백업/복원/hot-swap 워크플로 가이드 — 백업 생성·목록·복원·hot-swap·검증·정리를 한 콘솔에서 다루는 탭 셸과 "다음 액션" 가이드를 만든다(이 셸이 백업/복원 UI의 root). (Agent B, 의존: T-209)
- T-249 복원 위저드 UI — 모드 선택(new_database/replace_current)·manifest 미리보기·dry-run(T-232) 결과·replace_current typed confirmation·version/blocker 경고를 단계 위저드로 노출한다. (Agent B, 의존: T-248, T-232, T-234)
- T-250 hot-swap 전환 + maintenance window + source-verify UI — hot-swap plan/blockers·maintenance window 생성·typed confirmation·실행(T-241)·rollback·source 재검증 결과를 안전하게 노출한다. (Agent B, 의존: T-248, T-241)
- T-251 백업/복원 job 실시간 진행률(SSE) UI — 백업/복원 job의 단계·바이트·ETA를 SSE(+polling fallback)로 표시한다(T-203 SSE 패턴 재사용). (Agent B, 의존: T-248)
- T-252 백업 manifest/재현성 뷰어 + 만료/검증 컬럼 — manifest(source_match_set·release/snapshot·인벤토리 검증)와 expires_at·retention·verification·copy_count를 뷰어로 노출하고 active match set과 대조한다. (Agent B, 의존: T-248, T-237, T-240)
- T-253 복원 reconcile 결과 UI — 복원 후 row/MV/sppn/pt_source/source_set 항목별 PASS/FAIL(T-233)과 restore-drill(T-242) 결과를 노출한다. (Agent B, 의존: T-248, T-233)
- T-254 Admin 대용량 목록 가상화·필터·검색 — artifacts/jobs/sessions/reconcile 대형 목록을 filter/sort/search/pagination/virtualization으로 다루는 공통 컴포넌트(T-226과 공유). (Agent B, 의존: T-248)
- T-255 백업 Playwright e2e — 백업 생성·진행률·다운로드·삭제·취소·만료/검증을 mock API fixture로 고정(Chromium/Firefox). (Agent B, 의존: T-248, T-251)
- T-256 복원 Playwright e2e — 위저드 모드·manifest 미리보기·dry-run·replace_current 확인·blocker를 mock으로 고정. (Agent B, 의존: T-249)
- T-257 hot-swap Playwright e2e — plan·blockers·maintenance window·source-verify·rollback을 mock으로 고정. (Agent B, 의존: T-250)
- T-258 백업/복원 접근성·회복성 Playwright e2e — 신규 backups 위저드/hot-swap dialog의 키보드·focus trap·네트워크 오류·SSE 끊김·refresh 후 상태 복원을 검증(T-227 기존 표면과 중복 회피). (Agent B, 의존: T-255, T-256, T-257)

#### source-files 단계별 e2e (T-259~ · Agent B) — T-223 상위 spec의 단계 세분, fixture는 T-225

- T-259 단계별 e2e: 업로드 적재 — category 선택·기준월·multipart progress·register 성공 경로를 고정. (Agent B, 의존: T-225)
- T-260 단계별 e2e: 업로드 대기·재개·409 충돌 — 중단 업로드 재개 목록·resumable·slot replace·409 dialog를 고정. (Agent B, 의존: T-225)
- T-261 단계별 e2e: 구조 검증 — 성공/warning/failed 구분과 사유 표시를 고정. (Agent B, 의존: T-225)
- T-262 단계별 e2e: 매칭 세트 — match set 생성·검증(invalid/revalidatable)·활성화·integrity_alert를 고정. (Agent B, 의존: T-225)
- T-263 단계별 e2e: DB 입력 — rebuild-db enqueue·job progress(SSE/polling)·force_promotion 확인·실패 경로를 고정. (Agent B, 의존: T-225)

### 최하위 우선순위 (ID는 낮지만 순위 최하위)

- T-219 v1 VWorld 계약 정합·검증 보강 (T-106 #189 사후 리뷰 후속) — T-106 리뷰(PR #189 코멘트, 다중 에이전트 적대적 리뷰: 확정 H0/M5/L5)에서 나온 후속 부채를 정리한다. 런타임 wire 동작은 정확하므로 범위는 **published 계약(OpenAPI/생성타입) 정합**과 **테스트 커버리지**, 그리고 v2 side-effect 결정이다.
  - **M1(근본)**: 라우터가 raw `ORJSONResponse`(`vworld_success_response`) 반환 → `response_model`/`response_model_exclude_none`이 문서 전용·무효, 실 payload와 두 경로로 갈림. 생성 openapi.json 스키마에 대표 런타임 body(default/geocode simple/refine=false/reverse simple/NOT_FOUND 200/도메인 에러)를 검증하는 계약·회귀 테스트를 추가해 drift가 CI에서 깨지게 한다(이 한 건이 M2·M3·M5 상당수를 동시에 닫음).
  - **M2**: openapi.json/생성타입이 `simple/refine` 생략 필드(`input`/`refined`/`result[].type`)를 `required`+`additionalProperties:false`로 표기 → optional화(또는 success-variant 분리) 후 재생성. **wire 동작은 VWorld-correct이므로 변경 금지.**
  - **M3**: 새 400 VWorld error envelope(`VWorldErrorEnvelope/Body/Detail`)이 어떤 라우트에도 연결되지 않아 스펙에 없음(v1 geocode/reverse는 200+422만 광고). `responses={400: {"model": VWorldErrorEnvelope}}`(+429/5xx) 연결, 오해 소지 422 억제, openapi.json+api.gen.ts+schemas.gen.ts 재생성.
  - **M4(결정 필요)**: 본 PR이 추가한 전역 `RequestValidationError` 핸들러가 비-vworld 경로(v2/search/zipcode/pobox/admin)의 검증 에러를 FastAPI 기본 422 `{detail}` → 400 `{response:{errorCode,...,hint:str(exc.errors())}}`로 변경(ADR-053 범위 밖·미문서화, "v2 untouched" wire 위반). **택1**: (a) 의도적으로 400 `{response}`로 통일 → 새 ADR·문서·openapi.json 재생성·v2 회귀테스트 + `hint=str(...)` 대신 구조화 body, 또는 (b) override를 vworld 경로로 한정(`operation is None`이면 기본 핸들러로 위임)해 v2 등은 422 유지. **이 결정은 ADR/사용자 판단과 Codex 영역 조율이 선행**되어야 한다.
  - **M5**: 미테스트 라이브 분기 — reverse `simple=true`(input+각 `result[].type` 제거), geocode `refine=false`(refined만 제거), 도메인 에러(`INVALID_RANGE`/`INVALID_TYPE`/`OVER_REQUEST_LIMIT` 코드·레벨·텍스트 템플릿·`<param>` 보간), `NOT_FOUND` 200 success envelope, `PARCEL` casing. DB-free 테스트 추가.
  - **L**: Starlette 404/405 v1 unwrapped 누출, dead `VWorldErrorDetail` 모델(수동 dict로 빌드 중), 좌표 bounds 텍스트 영어 하드코딩(validation은 한글)·동일 `INVALID_RANGE` 불일치, v1 docs/ADR-053에 `service.version`/에러 `response.service`·`response.status` 명시.
  - 범위: v1 vworld 표면 owner는 Codex(T-106)이므로 M1~M3·M5(v2 무영향, openapi/typegen·테스트)는 Agent B가 PR 가능하나 착수 전 Codex와 영역 조율한다. M4는 결정 후 진행. v1 vworld 호환(ADR-038) 자체는 변경 금지. (의존: T-106 #189; M4는 ADR/사용자 결정 + Codex 조율 선행)
- T-105 v2 API 재audit·반영 — 호환성보다 **확장성·일관성·유지보수성** 기준으로 v2 재audit 후 반영. `docs/api-reference/v2/conventions.md` 신규(차원별 현재/목표) + ADR. 항목: 응답 envelope 일관(regions/within-radius 포함), `V2Source`/`match_kind` enum·dead value 정리, 페이지네이션(limit형 vs page형) 규약, v2 전용 error 모델, include_geometry/bbox 비대칭, 좌표/반경 네이밍·단위, GET/POST·버저닝·service 메타, OpenAPI/`api.gen.ts` 정합. v1 vworld 호환(ADR-038)은 **절대 변경 금지**. 각 변경은 backend+frontend type 동반. (의존: —, 우선순위 최하위)

## 보류 (외부 조건)

- T-119 serving 좌표 편입 — 출입구 후보 scoring. T-123에서 C11은 조건부 serving 후보로 유지했지만 ADR-051을 accepted로 전환하지 않았다. T-125 사전 검증은 `blocked / no-go`로 끝났으므로 T-129~T-134·T-137 후속 분석, Admin UI 반영(T-220/T-221), ADR-051 accepted, 사용자 승인이 모두 끝날 때만 진행한다. 출입구(C11)를 `mv_geocode_target` 대표 좌표 scoring에 feature flag로 편입하고, vworld 호환 필드는 유지하며 자체 정보는 `x_extension`/v2로 둔다. (의존: T-118 + T-125 + T-129~T-134·T-137 + T-220·T-221 + ADR-051 accepted + 사용자 승인)
- T-063 N150/Odroid 실측 실행 — 실제 N150/Odroid 장비가 준비되면 T-055 runbook을 사용해 full-load, SQL benchmark, REST benchmark, MV refresh/swap, backup/restore를 최소 3회씩 측정하고 `artifacts/perf/n150-vs-odroid-*`와 요약 문서를 남긴다. 하드웨어가 없으면 진행하지 않는다. 상세: `docs/t055-deployment-n150-odroid.md`

## 완료
- [x] T-236 외부/원격 대상 백업 안전 복사(A+B/Claude). 백업이 같은 디스크에만 있으면 디스크 사망 시 동반 소실(3-2-1 위배)이라, 기존 archive를 추가 대상으로 복사하는 비파괴 경로를 추가했다. `copy_backup_artifact`가 source를 `resolve_existing_archive_path`로 확인하고 대상을 `backup_copy_targets` allowlist(미설정 시 `backup_allowed_dirs` 재사용) 하위로만 허용(`resolve_backup_copy_target`, path-escape 거절·source==dest 거절·기존 파일 보호), `asyncio.to_thread(shutil.copyfile)`로 streaming 복사 후 **대상 sha256을 source와 재대조(불일치 시 대상 삭제+오류)**, 0600 권한 유지. RustFS/S3는 범위 밖. 표면: `POST /v1/admin/backups/{id}/copy`(`BackupCopyRequest`→`BackupCopyResult`, audit `db_backup.copy`), `client.copy_backup`, `ktgctl backup copy <id> <target_dir>`. settings `backup_copy_targets` 추가. 단위테스트 4건(복사+검증+0600/allowlist 밖 거절/손상 복사 monkeypatch 정리/copy_targets fallback). backend pytest/ruff/mypy/lint-imports KEPT/openapi --check 0·typegen 통과. (2026-06-16)
- [x] T-131 C11 guarded candidate policy 설계·오프라인 simulation. blanket C11은 candidate C4/C6/C7 오류 68/3,635/9,896으로 계속 no-go다. `centroid_c4_50_c6_c7_move_500`은 C3 결측 3,482,270건을 채우면서 candidate C4/C6/C7 오류 0건, p99 이동 64.981m, max 495.345m, 500m 초과 0건으로 T-132 반복 검증의 1차 후보가 됐다. 다만 100m 초과 이동이 10,099건 남아 active serving promotion은 금지한다. Artifact는 `F:\dev\geodata\t131-c11-guarded-policy-simulation\20260616-r1\`, 상세는 `docs/t131-c11-guarded-policy-simulation.md`. (2026-06-16)
- [x] T-235 복원 취소/실패 시 target DB 정리 정책(A+B/Claude). `t046:504` 미구현 항목. `run_restore_job`이 new_database 모드에서 `ensure_target_database_empty` 통과 시 `job_owns_target=True`로 표시하고, 취소(`CancelledError`)/실패 시 `_cleanup_restore_target_on_failure`로 정리한다. 순수 정책 `restore_target_cleanup_action`은 **replace_current면 항상 None(라이브 서빙 DB 절대 자동 정리 금지)**, job이 만든 빈 DB만 정책(quarantine|drop|keep, 기본 quarantine)에 따라 처리. `cleanup_orphan_restore_target`은 maintenance(postgres) AUTOCOMMIT 연결에서 `pg_terminate_backend` 후 `DROP DATABASE`/`ALTER DATABASE RENAME`을 수행하고 결과를 `db_restore.target_cleanup` audit에 기록(정리 실패는 원래 오류를 가리지 않게 best-effort suppress). settings `restore_failed_target_cleanup` 추가. 순수 정책 단위테스트 5건(replace_current 보호/unowned 무처리/quarantine·drop/keep·unknown). backend pytest/ruff/mypy/lint-imports KEPT/openapi --check 0 통과. 실 drop/rename·외부 비빈 DB 무변경 통합은 T-245. (2026-06-16)
- [x] T-233 복원 후 row_count 대조·정합성 reconcile(A+B/Claude). restore가 ANALYZE+smoke만 하고 manifest 대조를 안 하던(`t046:438` 권장 미구현) silent partial restore 위험을 메웠다. `infra/restore_reconcile.py`의 순수 `diff_restore_row_counts`(ROW_COUNT_OBJECTS 10개 object별 expected/actual/match, legacy=unknown은 비-mismatch)와 `compare_restore_against_manifest`(MV non-empty/parity·`tl_sppn_makarea`·`pt_source` 분포·`source_set` yyyymm까지 수집)를 추가하고, `run_restore_job`이 `run_row_count_check`(기본 true) 시 호출해 결과를 restore-log manifest `row_count_verification`에 기록한다(불일치=warning, legacy/MV 부재 graceful). 순수 diff 단위테스트 5건(전부 일치/1건 불일치 FAIL/actual 결측=0 FAIL/unknown expected 비-flag/None graceful). backend pytest/ruff/mypy/lint-imports KEPT/openapi --check 0·typegen 통과. 실 복원 DB reconcile(row 1건 삭제 주입 FAIL)은 T-244 통합. (2026-06-16)
- [x] T-234 복원 PostgreSQL/PostGIS major version mismatch hard-fail 가드(A+B/Claude). `t046:505` 미구현 항목. `run_restore_job`이 dump-dir 확인 직후(pg_restore 전) target cluster 버전을 조회해 `restore_version_mismatch_blocker`(T-232 `check_restore_version_compatibility` 재사용)로 manifest vs target의 PostgreSQL major·PostGIS major.minor 불일치를 검사하고, 불일치 시 기본 `InvalidInputError`로 차단한다. `allow_version_mismatch`(요청) 또는 `KTG_RESTORE_ALLOW_VERSION_MISMATCH`(설정)로 명시 우회, PostgreSQL minor patch(16.3 vs 16.4)는 허용. T-232 dry-run도 같은 정책으로 mismatch를 (우회 미설정 시) blocker로 보고하도록 정렬. `RestoreCreateRequest.allow_version_mismatch`·settings 추가. 가드 단위테스트 3건(major 차단/우회 허용/minor·동일 통과). backend pytest(11)/ruff/mypy/lint-imports KEPT/openapi --check 0·typegen 통과. 실 major-mismatch DB 차단 통합은 T-246. (2026-06-16)
- [x] T-130 C11 C4/C6/C7 회귀 샘플 원인 분석. C4 over500 68건은 candidate regression 52건, shared 16건으로 분리됐다. C6 candidate error 3,635건 중 candidate regression은 2,834건이고, 이 중 2,827건은 baseline 출입구가 없어 기존 C6 평가 대상이 아니던 row가 C11 후보로 새로 평가된 결과다. C7 candidate error 9,896건 중 candidate regression은 3,087건이고, 이 중 3,077건도 baseline 출입구 결측 row다. Artifact는 `F:\dev\geodata\t130-c11-regression-root-cause\20260616-r1\`, 상세는 `docs/t130-c11-regression-root-cause.md`. Windows Python cp949 GitHub JSON 디코딩 실패와 `gh pr merge` repo 생략 실패 패턴은 `docs/agent-failure-patterns.md`에 문서화했다. (2026-06-16)
- [x] T-232 복원 dry-run(preflight 전용, A+B/Claude). 실제 pg_restore 없이 복원 가능성을 미리 판정한다. `run_restore_dry_run`이 archive 해석+sha256(`verify_archive_checksum`), 임시 extract 후 내부 `checksums.sha256`+`manifest.json` 필수필드, target 복원성(new_database=빈 DB 확인/replace_current=현재 DB 일치), 버전 호환(`check_restore_version_compatibility`: PostgreSQL major·PostGIS major.minor 불일치)을 검사해 `can_restore`+`blockers`+`warnings`를 반환한다(extract temp는 finally 정리, **pg_restore 미실행·target 무변경**). 버전 헬퍼는 T-234 hard-fail 가드와 공유. 표면: `POST /v1/admin/restores/dry-run`(`RestoreCreateRequest`→`RestoreDryRunResult`, audit `db_restore.dry_run`), `client.restore_dry_run`. 버전 헬퍼 단위테스트 8건(동일/PG minor 허용/PG major·PostGIS major.minor 불일치/patch 허용/None/파서). backend pytest/ruff/mypy/lint-imports KEPT/openapi --check 0·typegen 통과. 전체 dry-run(실 archive+DB) 통합은 T-244. (2026-06-16)
- [x] T-231 백업 무결성 온디맨드 검증(quick/deep, A+B/Claude). checksum 검증이 restore 경로 안에서만 일어나 silent corruption(bit rot)을 복원 전 알 수 없던 문제를 메웠다. `verify_backup_artifact(artifact, settings, mode)`(`infra/backup.py`)가 비파괴로 archive sha256 재대조(quick) + 내부 `checksums.sha256` 대조·`manifest.json` 필수필드(database/backup/row_counts) 확인(deep, 임시 extract 후 정리)을 수행하고, **손상을 예외가 아니라 구조화 결과(`ok=False`+`errors`)로 반환**한다. 표면: `POST /v1/admin/backups/{id}/verify`(`BackupVerifyRequest`→`BackupVerifyResult`, audit `db_backup.verify`), `client.verify_backup`, `ktgctl backup verify <id> [--deep]`. quick-mode 단위테스트 5건(sha256 일치/불일치 구조화/storage_uri 없음/archive 없음/sha256 미기록=unknown). deep-mode의 손상 archive 검출은 T-245 fault-injection 통합에서. backend pytest/ruff/mypy/lint-imports KEPT/openapi --check 0·typegen 통과. (2026-06-16)
- [x] T-230 만료 백업 보존정책 janitor(A+B/Claude). T-229가 채운 `expires_at`을 소비하는 백업 전용 retention janitor를 추가했다. `infra/backup_janitor.py`의 순수 정책 `select_expired_backups`(최신 `keep_min_count` 보호·`pinned` 보호·`expires_at≤now`만 eligible)와 `run_backup_retention_janitor`(`BACKUP_JANITOR` advisory lock으로 직렬화, eligible 백업 archive unlink + `state='expired'` 전이 + `db_backup.expire` audit, 실패 격리). `--dry-run`은 파일 미변경·대상만 보고, lock 점유 시 `skipped_locked`. 표면: `POST /v1/admin/backups/janitor/run`(`BackupRetentionRunRequest`→`BackupRetentionResult`), `client.run_backup_retention_janitor`, `ktgctl backup janitor [--dry-run] [--keep-min N]`. settings `backup_retention_keep_min`(기본 3), 백업본은 재생성 가능하므로 파일 정리 기본 허용(ADR-052와 별개). 정책 단위테스트 5건(만료/pinned/keep_min 0·초과/미만료). backend pytest/ruff/mypy/lint-imports KEPT/openapi --check 0·typegen 통과. (2026-06-16)
- [x] T-229 백업 artifact expires_at·retention_class 기록(A+B/Claude). `admin_repo.update_artifact`(`admin_repo.py:682`)에 `expires_at`/`retention_class` 파라미터가 없어 모든 백업 artifact의 expires_at이 NULL로 남던 비대칭(insert는 지원)을 메웠다. `run_backup_job` 성공 finalize가 `artifact_expires_at(settings, req.retention_days)`(= finalize 시각 + retention_days, 미지정 시 `backup_artifact_ttl_days`=30)와 `retention_class='default'`(`DEFAULT_BACKUP_RETENTION_CLASS`)를 기록한다. 이로써 `ops.artifacts` expired 카운트와 T-230 보존 janitor가 동작할 근거가 생긴다. `artifact_expires_at` 단위테스트 4건. DB-level 검증(만료 카운트)은 round-trip 통합(T-244)에서. backend pytest/ruff/mypy/lint-imports KEPT 통과. (2026-06-16)
- [x] T-228 백업 preflight 디스크 여유 공간 사전 추정 가드(fail-fast, A+B/Claude). `run_backup_job`이 dump 시작 전(디렉터리 생성 전)에 `pg_database_size × backup_space_safety_factor(기본 1.3)`로 temp(dump)·destination(archive) 여유를 `estimate_backup_space_requirement`(`shutil.disk_usage`)로 추정하고, 부족하면 `InvalidInputError`로 fail-fast한다. temp·destination 동일 파일시스템이면 dump+archive 동시 점유를 합산. `KTG_BACKUP_REQUIRE_FREE_SPACE_CHECK=false`로 우회. settings에 `backup_space_safety_factor`/`backup_require_free_space_check` 추가. mock `disk_usage` 단위테스트 6건(추정·동일/다른 FS·zero·ancestor·defaults), `t046` 미구현 항목 갱신. backend pytest/ruff/mypy/lint-imports/openapi 0-drift 통과. (2026-06-16)
- [x] T-223 source-files e2e 교차흐름·스모크 상위 spec(Agent B). 기존 `/admin/source-files` Playwright spec(T-209/T-184, page.route 목킹)에 ① serving_usage 분류+현재 서빙 포함/미포함 구분(T-221/T-224: roadname=활용 중+현재 서빙 포함, epost=별도 기능 후보+현재 서빙 미포함+"등록됨 ≠ 활용 중") ② 6개 탭 교차 흐름 smoke(탭 선택 상태 유지)를 추가하고, 카탈로그 목킹에 serving_usage를 반영했다. 세트명 list+상세 중복으로 인한 strict-mode 다중매치는 `.first()`로 한정. **Windows Playwright chromium 8 + firefox 8 통과**(로컬; e2e는 CI 게이트 아님). 단계별 상세 e2e는 T-259~T-263. (2026-06-16)
- [x] T-224 파일 적재 UX 고도화(Agent B). T-221 serving_usage 분류 위에, UploadTab 각 카드에 활성 매칭 세트 기준 **동적 "현재 서빙 포함/미포함"** 배지(active match set의 non-omitted item cross-reference, `activeServingCategorySet`)와 "등록해도 활성화 전에는 서빙 미반영(등록됨 ≠ 활용 중)" 강조 안내를 추가했다. 기준월 입력·기대 멤버·serving_usage 금지선 note는 기존/T-221에서 제공. 프론트 type-check/lint/vitest(73)/build 통과. 남은 UX polish(카테고리 wizard·drag-and-drop validation preview·예상 DB 입력 영향 카드)는 후속 이슈 #201로 분리. (2026-06-16)
- [x] T-221 T-125 적재 변경점 Admin UI 반영(Agent B). T-220 감사가 찾은 백엔드 데이터 gap을 메웠다 — ADR-054 5분류 `serving_usage`(serving_core/validation_only/typed_feature_candidate/separate_feature_candidate/promotion_blocked_no_go)를 `SERVING_USAGE_BY_CODE` 단일 출처로 정의해 `SourceFileCategoryInfo`·카탈로그 API·client에 노출(openapi/typegen 재생성). UI는 UploadTab 카드에 serving_usage 배지(serving_core만 녹색, no-go는 red, 나머지 amber — `StatusBadge` tone 추가)와 ADR-054 금지선 note(국가지점번호=core.sppn 계산값, C11=T-125 no-go, 민원행정기관=대표좌표 대체 금지 등)를 노출하고, `구성 역할`을 별도 표기했다. 매칭/목록/현재구성 3탭의 `{item.role}` enum 원문을 `sourceRoleLabel`로 변환했다. `severityClass`를 비-string에 robust하게 보강. 백엔드 pytest/ruff/mypy/lint-imports, 프론트 type-check/lint/vitest(72)/build 통과. **잔여**: 업로드 그룹별 동적 "active serving 포함/미포함" 컬럼은 T-224(파일 적재 UX)로 위임. (2026-06-16)
- [x] T-220 실제 활용 파일 ↔ Admin UI 정합성 감사(Agent B). 다중 에이전트 grounding 감사(UI 라벨링/백엔드 카탈로그·DTO/ADR-054)로 `/admin/source-files`가 source의 serving 활용도를 오해시키는지 점검했다. **verdict: 오해 가능(NEEDS FIX → T-221)**. 근본 원인은 백엔드 데이터 gap — `SourceFileCategoryInfo`에 serving_usage/promotion_status 필드가 없고 카탈로그 `role`이 active match set과 무관한 정적값(`admin.py:259-272`), 역할 배지가 전부 녹색(`severityClass`), C11 T-125 no-go·국가지점번호 좌표=`core.sppn` 계산값·`roadaddr_entrance` 조건부 fallback이 어디에도 안 보임. H 2건(C11 no-go 미표시, 민원행정기관 '보강' 오해), M 4건, L 3건. source별 분류표·백엔드 gap·T-221 핸드오프 8항목·응답 구조 snapshot을 `docs/t220-admin-ui-source-usage-audit.md`에 정리. (2026-06-16)
- [x] T-129 C11 100m 초과 outlier 분류 및 원인 태깅. T-213 r3 DB와 T-125 C11 후보 기준으로 14,433건 전체를 자동 태깅했다. primary tag는 `candidate_coordinate_error=13,000`, `current_representative_error=899`, `source_month_drift_possible=287`, `key_mismatch=210`, `crs_or_source_coordinate_error=33`, `manual_review=4`다. 후보 202604와 텍스트 202605 기준월 차이는 전체 row의 secondary tag로 보존했고, 공간/key 신호와 결합될 때만 primary 원인으로 승격했다. Artifact는 `F:\dev\geodata\t129-c11-outlier-triage\20260616-r1\`, 상세는 `docs/t129-c11-outlier-triage.md`. (2026-06-16)
- [x] T-128 optional 원천 사용·미사용 최종 판정. PR #193/#194의 Claude Code clean-slate v2 분석을 반영하고, 두 PR의 comment/review/thread가 0건임을 확인했다. 최종 문서 `docs/optional-source-usage-decision.md`를 추가해 국가지점번호 좌표는 `core.sppn` 계산값으로 활용, `TL_SPPN_MAKAREA`는 context 유지, `도로명주소 건물 도형` C11 출입구 blanket 승격은 T-125 no-go로 보류, `상세주소DB`/`건물군 내 상세주소 동 도형`은 상세주소 typed feature 후보, `주소DB`/`건물DB`/`민원행정기관전자지도`/국가지점번호 grid·center는 검증·별도 기능 후보로 확정했다. ADR-054를 accepted로 추가하고 기존 검토 문서는 최종 판정 문서를 우선 참조하게 했다. (2026-06-15)
- [x] T-125 C11 serving 편입 승인 전 사전 검증. T-213 r3 DB(`kor_travel_geo_t213_20260615_r3`)에서 `roadaddr_building_shape_bundle` C11 후보를 staging으로 만들고 기존 `mv_geocode_target` 대표점과 비교했다. Coverage는 matched 6,404,009건, current-only 15,786건, candidate-only 2,156건이다. 거리 impact는 p50 `2.348m`, p95 `22.801m`, p99 `54.283m`, 100m 초과 14,433건이라 ADR-051 초안 임계값을 넘었다. C3 결측은 3,513,854건에서 15,786건으로 개선됐지만 C4 over500 16→68, C6 ERROR 803→3,635, C7 ERROR 6,815→9,896으로 악화되어 gate는 `blocked / no-go`다. Artifact는 `F:\dev\geodata\t125-c11-serving-preflight\20260615-r2\`, 상세는 `docs/t125-c11-serving-preflight-result.md`. ADR-051은 proposed 유지, T-119는 계속 보류. (2026-06-15)
- [x] T-218 T-215 acceptance 독립 재실행(Agent B). PR #185 접속 정보로 live T-213 r3 DB(`kor_travel_geo_t213_20260615_r3`)에 붙어 T-215 phase ② 최종 검증 acceptance를 read-only로 재현했다. Preflight(`mv_geocode_target`=`mv_geocode_text_search`=6,419,795, `tl_sppn_makarea`=24,204, active release `54e17e80…`, custom match set `0c7d7ee7…` core 6+augment 8)는 T-215/T-216 baseline과 일치하고, C1~C10(`run_all_cases`)은 `severity_max=ERROR`이며 각 case count가 T-215(#183)와 byte-identical(C1 33,897 / C2 32,496 / C3 3,513,854 / C4 3,416 / C5 202 / C6 803 / C7 6,815 / C8 24,483 / C9 0 / C10 7)이다. v1/v2 smoke 7종은 전부 200/`OK`(v1 reverse 첫 후보 `경기도 용인시 수지구 성복1로 35` round-trip 일치)이고, C11~C17은 match set 구조(augment 8 category)로 runnable을 재확인했다(전체 run-validation은 공유 baseline write 회피 위해 T-216 #187 결과 인용). 성능(SQL c64)은 T-217에서 별도 무회귀 확인. 상세: `docs/t218-t215-acceptance-rerun.md`. (2026-06-15)
- [x] T-217 T-214 SQL benchmark 독립 재실행(Agent B). PR #185 접속 정보로 live T-213 r3 DB(`kor_travel_geo_t213_20260615_r3`, mv 6,419,795 식별성 검증)에 하네스 TCP 접속을 확인하고, `benchmark_query_performance.py`를 T-214 동일 설정(cases-per-group=100, iterations=3, warmup=1, concurrency 1/4/16/64, pool 20/64, statement_timeout 5s)으로 read-only 재측정했다. query group 8종(Q1~Q8), total error 0, worst c64 p95 `Q4_SEARCH/search_fuzzy=268.370ms`로 T-214(`245.895ms`)~T-215(`308.617ms`) band 내·무회귀를 독립 확인했다. REST는 T-216(#187, `415.022ms`≤`534.031ms`)에서 수용 완료라 재실행 생략, MV swap/RustFS reconcile은 공유 baseline DB write·RustFS 비활성으로 제외했다. 상세: `docs/t217-t214-sql-rerun.md`. (2026-06-15)
- [x] T-207 epost 수동 server-fetch(pobox/bulk) — `/v1/admin/source-files/epost-fetch`와 업로드 탭 `epost 받기` 버튼을 추가했다. 서버가 epost ZIP을 내려받아 사서함/다량배달처 텍스트 파일을 찾고, T-120 검증 모듈로 검증한 뒤 RustFS `single_file` source registry에 등록하고 `pobox_load`/`bulk_load` job을 enqueue한다. fetch 실패/ZIP 구조 불일치/기준월 mismatch는 upload session failed state와 warning payload로 남기며, 자동/스케줄 다운로드와 핵심 rebuild 편입은 범위 밖으로 유지한다. 상세: `docs/t207-epost-server-fetch.md` (2026-06-15)
- [x] T-106 v1 vworld geocoding/reverse 호환 재점검·코드 반영. ADR-053으로 호환 수준을 "REST v1 geocode/reverse의 VWorld HTTP envelope/key/대소문자 호환 + `x_extension` 유지"로 확정했다. `/v1/address/geocode`와 `/v1/address/reverse` 정상 응답은 `{"response": ...}`로 감싸고 `service.name=address`, `operation=getCoord/getAddress`, 응답 `type` 대문자 직렬화, geocode `simple`/`refine` 생략 규칙, reverse `simple` 파라미터, VWorld식 `error.level/code/text` 요청 검증 에러를 반영했다. OpenAPI와 UI 생성 타입을 갱신하고 DB 없는 v1 HTTP 회귀 테스트를 추가했다. (2026-06-15)
- [x] T-216/T-126 phase ② live acceptance. RustFS endpoint/bucket 접근을 확인한 뒤 T-213 기준 DB `kor_travel_geo_t213_20260615_r3`에서 optional source 8개 category/40개 archive를 source registry에 등록했다. base source match set `a0c2d514-a91d-44c4-bdb6-0bc4771ae61a`에 optional group을 더한 custom source match set `0c7d7ee7-75bf-4a1e-ae0b-015485e73656`으로 C11~C17 run-validation을 실행했고 `runnable=7`, `skipped=0`, `failed=0`이다. REST c64 동일 표본 425 case 재측정은 error 0, worst p95 `Q4_SEARCH/search_hint=415.022ms`로 T-214 기준 `534.031ms` 이하라 수용했다. 공급자 원본 SHP 파일명 token에서 layer명을 추출하도록 `source_member_scan`도 보강했다. Artifact는 `F:\dev\geodata\t216-acceptance\20260615-r2\`, 상세는 `docs/t216-live-acceptance.md`. (2026-06-15)
- [x] T-215 phase ② 튜닝·최종 검증 평가. T-213 r3 전용 baseline(`kor_travel_geo_t213_20260615_r3`, serving release `54e17e80-312e-46da-a58f-d8b10be37c85`)에서 preflight, C1~C10 재실행, C11~C17 run-validation, v1/v2 geocode/search/reverse/zipcode smoke, SQL/REST c64 재측정을 수행했다. Preflight와 v1/v2 smoke는 통과했지만 C1~C10은 known data-quality 상태로 `severity_max=ERROR`이고, C11~C17은 현 serving match set에 보강 원천이 없어 7건 모두 `skipped`다. SQL c64 error는 0건, worst p95는 `Q4_SEARCH/search_fuzzy=308.617ms`다. REST c64 sample은 error 0건이지만 pool `20/64`에서도 worst p95가 `Q3_FUZZY_GEOCODE/geocode_fuzzy_hint=3631.900ms`로 T-214보다 크게 악화되어 성능 수용은 T-126으로 분리했다. Artifact는 `F:\dev\geodata\t215-acceptance\20260615-r1\`, 상세는 `docs/t215-phase2-final-acceptance.md`. (2026-06-15)
- [x] T-209 Admin UI `/admin/source-files`. 5탭(업로드·목록·매칭·reconcile·현재 구성) + 동적 검증 케이스 탭, category 카드·multipart progress·재개 목록·409 dialog, integrity_alert, 현재 구성 탭 T-208 파생 표시(`restored_from_backup`/`source_file_unavailable`/`알수없음`/`추정`), reconcile 탭 T-212 "정리 대상" 일괄 hard-delete + capacity over-threshold 경고, 업로드 탭 epost 받기(T-207) 버튼·라이브 SSE 진행률(`source_upload.progress` EventSource + polling fallback)·인라인 에러·per-card pending, full-prefix rename(api.gen.ts), 프론트 단위 테스트 + Playwright e2e(`tests/e2e/source-files.spec.ts`, chromium/firefox 통과). PR #156/#167/#170/#176 + e2e. (2026-06-15)
- [x] T-214 phase ② 성능평가·벤치. T-213 r3 전용 baseline(`kor_travel_geo_t213_20260615_r3`, RustFS `kor-travel-geo/t213/20260615-rerun3`, serving release `54e17e80-312e-46da-a58f-d8b10be37c85`)을 preflight로 확인한 뒤 full-load/rebuild-db 로그, T-047 SQL benchmark, REST API benchmark, T-035 MV refresh/swap benchmark, source registry deep rehash/multipart synthetic benchmark, RustFS quick/deep reconcile을 실행했다. SQL/REST benchmark error는 0건이고, SQL c64 worst p95는 `Q4_SEARCH/search_fuzzy=245.895ms`, REST c64 worst p95는 `Q4_SEARCH/search_fuzzy=534.031ms`다. MV는 `concurrent=126.414s`, `swap-rerun=340.128s`이며 row count는 유지됐다. Reconcile은 등록 DB file missing 없이 prefix orphan object warning 2건(`object_missing_db`)만 남겼다. Artifact는 `F:\dev\geodata\t214-benchmark\20260615-r3\`, 상세는 `docs/t214-phase2-performance-benchmark.md`. (2026-06-15)
- [x] T-213 phase ② 전국 라이브데이터 로딩. `scripts/run_t213_live_pipeline.py`를 추가해 전국 실 원천 archive 6종을 RustFS upload/register → source match set validate/activate → rebuild-db 경로로 실행했다. `source_match_set_id=6eb2b07b-f34f-460a-91ab-a5847a1e979e` 기준 source load 6개 job은 모두 성공했고, post-load recovery로 consistency report `consistency_7238f3fb50e347ccb8b3c6808402e656`와 `mv_refresh`를 완료해 active serving release `96e60a10-695c-4a45-ad26-91422eb2f855`를 생성했다. 최종 row count는 `mv_geocode_target=6,419,795`, `mv_geocode_text_search=6,419,795`, `tl_sppn_makarea=24,204`다. 실행 중 발견한 batch consistency timeout, fresh rebuild DB의 MV 없는 sample 보강, 전자지도 staging parent 처리, `registered` upload session terminal state 누락을 보강했다. 초기 batch root는 실패 이력을 남기지만 active release와 snapshot FK는 정상이다. 상세: `docs/t213-phase2-live-loading.md` (2026-06-15)
- [x] T-212 RustFS 원천 archive 보존·정리 정책(ADR-052) + 관리 표면. 등록 archive 자동 삭제 금지 원칙 하에 `destructive_admin` typed-confirmation(`HARD-DELETE-SOURCES`) 기반 수동 일괄 hard-delete(`POST /v1/admin/source-files/bulk-hard-delete`: active-정본 guard + manifest/ack 게이트 + `source.hard_delete` audit + `kor_travel_geo_source_hard_deletes_total` metric)와 capacity retention recommendation을 구현하고, reconcile 탭 "정리 대상" 일괄 삭제 다이얼로그 + capacity over-threshold 경고(retention recommendation UI 노출) + 구조화된 삭제 결과 요약을 UI에 추가했다. bulk restore·archive tier 전환은 ADR-052에서 명시적 범위 밖. PR #158(백엔드)/#167(다이얼로그)/#170(UI 경고). (2026-06-14)
- [x] T-211 source registry 관측성(metric/용량). upload session state·janitor abort/`registration_expired` 전이·reconcile issue_type·저장소 용량(category별 object/byte·30일 증가량·quarantined/soft_deleted/미등록) prometheus gauge/counter를 추가하고, source-files admin UI에 용량/이슈 요약 카드를 노출했다. PR #157. (2026-06-14)
- [x] T-208 backup/restore manifest + restored_from_backup. backup manifest에 source_match_set block을 넣고, restore 후 `restored_from_backup` stub lifecycle(두 상태머신 분리, hash 산출 후 M-A 옵션2 revalidatable)과 restore entrypoint별 source 검증 매트릭스(pg_restore / ADR-036 rename hot-swap 직후 quick reconcile)를 구현했다. PR #154. (2026-06-14)
- [x] T-206 consistency case registry 정식화 + C11~C17 + dynamic UI. `consistency_case_definitions/inputs` seed, `GET /v1/admin/consistency/case-definitions`, run-validation(사용 직전 무결성 게이트·failed vs skipped·`validator_version` 변경 시 재검증 표시), UI 동적 case tab(C1~C10 하드코딩 제거)을 구현했다. PR #153. (2026-06-14)
- [x] T-205 match set builder + rebuild bridge. match set CRUD/validate(active validate-in-place로 `integrity_alert` 해제 포함)/activate atomic swap(advisory lock, 외부 관찰 active gap 금지)/retire, `source_set_hash`, invalid·revalidatable 복구, rebuild-db(무결성 게이트·forced_promotion audit·source integrity 우회 불가), `dataset_snapshots.source_match_set_id` FK·rollback swap. PR #151(a)/#152(b). (2026-06-14)
- [x] T-204 RustFS reconciliation. quick/deep(+경과 강제/rolling deep), 12 issue_type, resolve(read-after-write·restore_soft_deleted·retry_delete·extend_deadline·duplicate active 정본 guard), capacity preflight, bucket 전체/prefix 대량 손상 시 `source_file_unavailable`+integrity_alert/invalid 전파. PR #149. (2026-06-14)
- [x] T-203 upload session 코어. RustfsClient 확장(head/delete/multipart/ListParts/rehash), session 생성/재개 + multipart/resumable + `failed_storage_state` + 409 + slot replace, 전자지도(11 layer)/navi(7z) validator, register(group_sha256) + `recompute_group_aggregates` 단일 service, advisory-lock janitor, soft-delete/restore, upload-session SSE events endpoint. PR #139(a)/#142(b)/#145(c). (2026-06-14)
- [x] T-202 admin 권한 모델. trusted proxy `X-KTG-Actor`/`X-KTG-Roles` → `RequestContext` → `require_role` 4종, GeoIP gate가 role gate 선행, 헤더 부재/미인식 role 403, source 관리 액션 audit `event_type` 집합·`actor_type` CHECK(`system:<job_kind>` 수용). PR #135. (2026-06-14)
- [x] T-201 category catalog + DTO + guess_source_kind 제거. 6 build + optional/enrichment category catalog, Source* DTO, `GET /v1/admin/source-file-categories`. legacy `/v1/admin/uploads` + 공개 upload-set client 메서드·`ktgctl load full-set` CLI 제거(breaking, changelog/migration-guide 명시). PR #136. (2026-06-14)
- [x] T-200 ops 스키마/migration 기반 + full-prefix ID rename(BREAKING). `ops.source_*` DDL을 Alembic + `infra/sql.py` SCHEMA_SQL + `sql/ddl` 3중 작성(명시 constraint/index·`char_length()`), `dataset_snapshots.source_match_set_id` FK·`state=available`↔`validation_state` CHECK·`integrity_alert`, full-prefix `ops` ID rename(snapshot_id→dataset_snapshot_id 등; admin/DTO/OpenAPI/UI/CLI 동반, CHANGELOG/migration-guide). fresh init-db↔Alembic head drift 게이트 포함. PR #134(schema)/#159(rename). (2026-06-14)
- [x] T-124 T-110~T-123 Claude 리뷰 후속 반영. PR #137/#138/#140/#141/#143/#144/#146/#147/#148/#150/#155/#160/#161의 Claude post-merge review와 review thread를 재확인했고 unresolved inline thread는 0건이었다. 블로킹 이슈는 없었지만 비차단 코멘트를 반영해 공통 SHP/DBF 파일·ZIP iterator와 C15 POI reader를 stream 경로로 바꾸고, C12 `road_geometry_missing`, C13 상세주소 숫자 필드 조기 검증, C14 deleted DBF row 제외 row count를 추가했다. T-121 runner는 C17 분기와 순차 staging 전제를 명시했고, T-122 benchmark는 sampler 종료 timeout, 비-kB RSS 파싱, Markdown 표 helper, opt-in 테스트를 보강했다. T-123 리뷰의 C12/C16 중복 유틸은 `augment_harness` import로 일원화했고, T110~T118 prototype 문서에 key overlap, full/weak key, tolerance, row count, sample SQL 해석 경계를 보강했다. (2026-06-15)
- [x] T-123 phase ① 튜닝·최종 검증 평가. `augment_harness`에 staging key index/`ANALYZE` helper를 추가하고 C11/C12/C16 prototype에 적용했다. T-122 cold materialization 기준선과 T-123 warm-cache 재측정을 분리했고, WSL ext4 테스트 미러 `artifacts/perf/t123-phase1-tuned-live/`에서 C11~C17 전체를 3090.891초에 실패 0건으로 완료했다. C11은 bundle ↔ 전자지도 full key 6,405,305건 거리 p95/max 0.0m로 조건부 serving 후보를 유지하지만, 기존 대표점 대비 impact, C3/C4/C6/C7 회귀, T-047/T-214 계열 성능 회귀, feature flag rollback, v1/v2 노출 정책 검증이 아직 없어 ADR-051은 proposed로 유지하고 T-119는 보류한다. C12~C17은 검증 전용으로 최종 확정했다. 상세: `docs/t123-phase1-acceptance.md` (2026-06-14)
- [x] T-122 phase ① 보강 성능평가·벤치. `scripts/benchmark_phase1_augment_performance.py`를 추가해 T-121 runner를 재사용하면서 preparation(materialization)과 C11~C17 case별 wall-time, runner process RSS, `/proc/self/io` I/O delta를 artifact로 기록했다. 전국 실행은 WSL ext4 테스트 미러 `artifacts/perf/t122-phase1-live/`에서 3961.937초에 완료됐고, preparation은 848.988초/16.0GiB local write, C11은 1284.931초, C12 peak RSS는 2.2GiB였다. 상세: `docs/t122-phase1-augment-benchmark.md` (2026-06-14)
- [x] T-121 phase ① 전국 라이브데이터 보강 실행. T-111~T-117 prototype을 `F:\dev\kor-travel-geo\data\juso` 전국 실 원천으로 실행해 C11~C17 `AugmentReport`와 `source_yyyymm`을 산출했다. C11 full key bundle ↔ 전자지도 거리는 p95/max 0.0m이고, C12~C17은 전국 key overlap·coverage·거리 metric을 artifact로 남겼다. C13 실제 상세주소 동 도형의 `MULTIPOLYGON`과 C15 staging SQL type은 prototype 보정으로 반영했다. 상세: `docs/t121-phase1-live-augment.md` (2026-06-14)
- [x] T-120 epost 우편번호 수동 적재·검증(pobox/bulk, CLI). 사서함·다량배달처 텍스트 파일 검증 모듈을 `epost_validation`으로 분리해 T-207 server-fetch 흐름에서 재사용할 수 있게 했다. `ktgctl load pobox`, `load bulk`, `load epost`는 적재 전 행수·필수 컬럼·우편번호 형식·사서함 종류·정수 필드·중복 key sanity 요약을 출력하고 실패 시 COPY를 실행하지 않는다. `download_epost_zip()`은 직접 ZIP 응답과 `fileLocplc` XML 응답을 모두 처리한다. 상세: `docs/t120-epost-postal-validation.md` (2026-06-14)
- [x] T-118 phase 1 go/no-go 종합 + serving 편입 ADR 게이트 문서. T-111~T-117을 source별로 "검증 전용 / 조건부 serving 후보"로 판정했고, C11만 ADR 승인·전국 metric·feature flag·기준월 gate를 전제로 한 조건부 후보로 남겼다. C12~C17은 phase ② registry/운영 검증 전용으로 정리했다. 전자지도 잔여 layer `TL_SPBD_EQB`는 구조 검증 필수 evidence이지만 serving 후보가 아니며, 필요하면 후속 C18 또는 C13 확장으로 분리한다고 결정했다. ADR-051은 proposed 초안으로 추가했고 T-119는 사용자 승인 전까지 보류한다. 상세: `docs/t118-phase1-go-no-go.md` (2026-06-14)
- [x] T-117 C17 내비 지번 member coverage 검증 prototype. `navi_full` 내부 optional member인 `match_jibun_*.txt`를 독립 category로 만들지 않고 staging table에 key만 COPY해 `tl_juso_parcel_link`와 `bd_mgt_sn+pnu`, `pnu+road key` coverage를 비교한다. member가 없으면 실패가 아니라 `skipped`로 기록하고, 7z는 register/materialization 단계에서 해제된 디렉터리를 입력으로 받는 계약을 유지한다. 좌표 적재 없이 `coordinate_load=False`, `serving_promotion=False`를 고정했다. 상세: `docs/t117-navi-jibun-coverage.md` (2026-06-14)
- [x] T-116 C16 주소DB/건물DB row·key drift 검증 prototype. `주소DB_전체분`/`건물DB_전체분` ZIP text member를 streaming으로 읽어 검증 전용 staging table에 key만 COPY하고, `tl_juso_text`/`tl_juso_parcel_link`/`tl_spbd_buld_polygon`과 distinct key overlap 및 `EXCEPT` 기반 left/right-only sample을 산출한다. 주소DB ZIP의 mojibake member 이름은 `cp437`→`cp949`로 복원하고, 좌표 적재 없이 `coordinate_load=False`, `serving_promotion=False`를 고정했다. 상세: `docs/t116-address-building-drift.md` (2026-06-14)
- [x] T-115 C15 민원행정기관 POI 거리 검증 prototype. `민원행정기관전자지도`의 CP949 한글 DBF field name과 SHP point를 읽어 staging하고, `도로명주소`를 기존 parser로 해석한 뒤 `mv_geocode_target` exact road geocoder 계약과 batch join해 POI 좌표와 geocoder 대표점 거리 p50/p95/max, geocode missing, parse failed, outlier sample을 산출한다. 주소 정본/일반 후보에는 혼입하지 않고 `serving_promotion=False`를 고정했다. 상세: `docs/t115-civil-service-poi.md` (2026-06-14)
- [x] T-114 C14 국가지점번호 grid/center 검증 harness. `국가지점번호 도형` 100km/10km/1km/100m SHP/DBF와 `국가지점번호 중심점` TXT를 상시 적재 없이 streaming 검증하는 모듈을 추가했다. Grid prefix에서 EPSG:5179 bbox/center를 계산하고, SHP bbox·중심점 좌표·`core/sppn.py` formatter parent prefix·resolution별 row coverage를 검증한다. `TL_SPPN_GRID_100M` 1천만 polygon은 ZIP member 전체를 inflate하지 않는 전용 iterator로 sampling/full stream 모두 가능하게 했고, 10m 좌표 개선 원천이 아니라 parser/formatter 회귀와 overlay 후보 검증 전용(`serving_promotion=False`)으로 고정했다. 상세: `docs/t114-national-point-grid.md` (2026-06-14)
- [x] T-113 C13 상세주소 동 containment 검증 prototype. `TL_SGCO_RNADR_DONG` polygon, `TL_SPBD_ENTRC_DONG` point, `상세주소DB` `adrdc_*.txt`를 staging/streaming 검증에 연결했다. TXT에는 좌표가 없으므로 `ST_Covers`는 `SIG_CD + BUL_MAN_NO`로 join한 동 출입구 point containment를 측정하고, 상세주소DB는 `BD_MGT_SN`/도로명주소 연계키 overlap과 address-matched coverage context로 사용한다. 측정 전용이며 serving SQL은 변경하지 않는다. 상세: `docs/t113-detail-dong-containment.md` (2026-06-14)
- [x] T-112 C12 건물 도형 connection line 검증 prototype. `TL_SPOT_CNTC` connection line을 staging 적재하고 전자지도 `TL_SPRD_MANAGE` 도로 관리선과 `RDS_SIG_CD + RDS_MAN_NO` key overlap, line-to-line 거리 분포, road key 결손 또는 tolerance 초과 dangling connection을 측정한다. 측정 전용이며 운영 C8/serving SQL은 변경하지 않는다. 상세: `docs/t112-c12-connection-lines.md` (2026-06-14)
- [x] T-111 C11 출입구 원천 간 거리 검증 prototype. 건물 도형 bundle `TL_SPBD_ENTRC`와 전자지도 `TL_SPBD_ENTRC`는 `ENTRANCE_KEY_FIELDS` full key로, `tl_locsum_entrc`/`tl_roadaddr_entrc`는 운영 테이블 제약상 `sig_cd + ent_man_no` weak key로 key overlap 및 `ST_Distance` 분포를 측정한다. `AugmentReport` payload를 제공하되 serving 좌표 승격은 하지 않는다. 상세: `docs/t111-c11-entrance-sources.md` (2026-06-14)
- [x] T-110 보강 검증 공통 harness. `src/kortravelgeo/loaders/augment_harness.py`에 시도 17개 group 순회 driver, `AugmentReport`, SHP `Point`/`PolyLine`/`Polygon` body parser, DBF row alignment, ZIP layer feature iterator, staging table 생성/COPY helper, key join 기반 `ST_Distance`/`ST_Covers` 측정 helper를 추가했다. PostGIS smoke는 `KTG_SLOW_REAL_DATA=1` + `KTG_TEST_PG_DSN` 선택형 테스트로 분리했다. 상세: `docs/t110-augment-harness.md` (2026-06-14)
- [x] T-108 — 운영 배포 자동화 (Sprint 6) — **Odroid M1S + N150 16GB 양쪽**
  (`pinvi` 원문에서 가져왔으며, 사용자 추가 지시에 따라 streaming replication은 제외).
  `pinvi`의 T-108 원문을 가져와 등록했고, 이 저장소에서는 DB/RustFS 생명주기
  직접 관리 금지 원칙을 지키며 API/UI 멀티플랫폼 이미지 buildx 계획·빌드·SSH 배포
  자동화를 `scripts/deploy_app.py`로 구현했다. 상세: `docs/t108-deploy-automation.md`.
  (2026-06-13)
- [x] T-077 `kor-travel-geo` 식별자 전환. Python 배포명과 GitHub URL 참조는 `kor-travel-geo`, import root는 `kortravelgeo`, 권장 alias는 `import kortravelgeo as ktg`, CLI는 `ktgctl`, 환경변수 prefix는 `KTG_*`, PostgreSQL 기본 DB명은 `kor_travel_geo`, RustFS bucket/prefix 기본값은 `kor-travel-geo`, UI package는 `kor-travel-geo-ui`로 통일했다. API 요청 성능 metric/logging을 추가하고, 이전 이름 계열 전수조사 2회와 Python/UI/Docker smoke 검증을 완료했다. 상세: `docs/t077-kor-travel-geo-rename.md`, ADR-047. (2026-06-13)
- [x] T-076 RustFS 업로드 저장소 접속 설정. upload set 저장소를 `local`/`rustfs`로 선택할 수 있게 하고, RustFS 설정·연결 확인·prefix import·기존 로컬 파일 sync API와 admin UI를 추가했다. 이 프로젝트는 RustFS를 직접 구동하지 않고 이미 동작 중인 bucket의 접속 설정만 사용한다. Windows Playwright Chromium/Firefox e2e를 통과했다. 상세: `docs/t076-rustfs-upload-storage.md`, ADR-044/ADR-045. (2026-06-03, 2026-06-10 갱신)
- [x] T-075 `/v2/regions/within-radius` DB 튜닝. `region_radius_parts` serving accelerator를 추가해 시도·시군구·읍면동 polygon을 `ST_Subdivide`로 쪼개고, 단일 SQL/단일 좌표 변환/parent code 필터로 반경 후보를 조회한다. 실제 T-027 DB에서 accelerator row count와 원본 `tl_scco_*` 직접 조회 결과 일치 테스트를 확인했고, `seoul_20km` p95는 122.05ms → 28.61ms, `busan_10km` p95는 45.80ms → 14.89ms로 줄었다. ADR-043. (2026-06-02)
- [x] PR #114~#115 리뷰 감사와 실제 DB 테스트 보강. PR #114부터 최신 PR #115까지 conversation/review/thread 표면을 확인했고 코멘트 0건을 기록했다. `regions_within_radius`는 선택형 실제 PostgreSQL 테스트를 추가해 실제 `tl_scco_*` 행정구역 도형 기반 contains 결과를 검증한다. 상세: `docs/postmerge-review-fixups-pr114-pr115.md` (2026-06-02)
- [x] T-073 T-027/T-047 국가지점번호 포함 재적재·튜닝 재측정. 새 Docker DB `kor-travel-geo-t027-retune`(port `15435`)에서 전체 적재와 20260401~20260506 daily 전체 적용을 재실행했다. 최종 `mv_geocode_target=6,418,735`, `mv_geocode_text_search=6,418,735`, `tl_sppn_makarea=24,204`이며, T-047 SQL benchmark는 2,000 case/18,000 measurement/error 0이었다. Q11 c64 p95는 `sppn_geocode=90.22ms`, `sppn_reverse=87.45ms`다. 상세: `docs/t027-t047-sppn-retune-20260601.md` (2026-06-01)
- [x] T-072 NTFS main repo와 에이전트 worktree 전환. Git source of truth를 `/mnt/f/dev/kor-travel-geo` 계열 NTFS checkout으로 두고, 테스트와 장기 실행은 WSL ext4 테스트 미러로 복사해 수행하는 정책으로 바꿨다. `/mnt/f/dev/kor-travel-geo-codex`, `/mnt/f/dev/kor-travel-geo-claude`, `/mnt/f/dev/kor-travel-geo-antigravity` worktree를 만들고 local secret/env 파일과 CodeGraph 인덱스를 준비했다. ADR-041과 개발 문서를 갱신했다. (2026-05-31)
- [x] T-067 디버그 UI point+geometry overlay 및 v2 geocode 도형 옵션 공식화. `/v2/geocode`에 `include_geometry` 옵션을 추가해 기존 대표점(`point`)을 유지하면서 행정구역/도로/건물 도형을 `geometry`/`bbox`로 함께 반환한다. `/debug/geocode`는 기본으로 이 옵션을 켜고 point marker + GeoJSON overlay를 지도에 표시하며, geocode/reverse 화면은 응답을 입력 아래에 두고 지도를 크게 배치한다. 실제 Docker DB에서 `성복동`→행정구역 polygon, `성복1로`→도로 line, `성복1로 35`→건물 polygon을 확인했다. 상세: `docs/t067-v2-geocode-geometry-overlay.md` (2026-05-30)
- [x] T-066 Consistency 탭 진입 프리즈 완화. `/admin/consistency` 진입 직후 첫 point sample을 자동 지도 로드에 사용하던 흐름을 끊고, 사용자가 sample을 선택하기 전에는 가벼운 placeholder만 렌더한다. `LazyCoordinateMap`은 sample 선택 후에만 로드되도록 `ConsistencyPanel`을 수정하고 unit test로 고정했다. 상세: `docs/t066-consistency-tab-freeze.md` (2026-05-30)
- [x] T-065 내비게이션용DB `시군구용건물명` 검색 반영. `match_build_*.txt`의 20번째 컬럼을 `sigungu_buld_nm`으로 적재하고 `sigungu_buld_nm_nrm`을 `mv_geocode_target`/`mv_geocode_text_search` 검색 후보에 포함했다. 실제 Docker DB에서 NAVI 10,687,317행 재적재, helper MV 재생성, `엄마집`/`P-101동` + `sig_cd=26110` 전후 recall 개선과 latency를 기록했다. 상세: `docs/t065-navi-building-name-search.md` (2026-05-30)
- [x] T-064 상위 주소 geocode 후보. `/v2/geocode`에서 상세번호가 없는 상위 주소 입력은 별도 endpoint 없이 `district` 검색 후보로 승격한다. `tl_scco_ctprvn/sig/emd/li` polygon의 `ST_PointOnSurface` 대표점을 사용하고, `수지구` 실제 DB smoke에서 첫 후보 `용인시 수지구(sig_cd=41465)`를 확인했다. 상세: `docs/t064-region-only-geocode.md` (2026-05-30)
- [x] Python 라이브러리 API v2 단일화. `AsyncAddressClient.geocode/reverse/search`를 후보 목록 응답으로 승격하고, 공개 Python API에서 `*_v2` 접미사와 v1-style `reverse_geocode`를 제거했다. REST `/v1/*`는 내부 어댑터로 vworld 호환 응답을 유지한다. ADR-039와 API reference를 갱신했다. (2026-05-29)
- [x] PR #69~#86 post-merge 리뷰 audit/fixup. PR #69부터 최신 PR #86까지 conversation/review/latestReview와 GraphQL review thread를 다시 확인했고, 대상 PR 모두 thread 0건이었다. PR #84 리뷰 후속으로 GeoIP gate를 admission control보다 먼저 실행하도록 middleware 순서를 바꾸고, `testclient` 특별 허용을 제거했으며, `X-Forwarded-For`의 `host:port`/`[IPv6]:port` 파싱을 보강했다. 상세: `docs/postmerge-review-fixups-pr69-pr86.md` (2026-05-29)
- [x] T-027 최종 실 데이터 클린 재적재 검증. 새 Docker compose project `kor-travel-geo-t027-final`과 전용 `pgdata-final-20260529`에서 실제 전국 `data/juso`와 `20260401_dailyjusukrdata.zip`을 처음부터 적재했다. 전체 3,963초, `mv_geocode_target=6,416,642`, `mv_geocode_text_search=6,416,642`, `tl_sppn_makarea=24,204`, active serving release `faa1f42b-f5b9-4ef0-af0b-1a422d938ed3`, smoke `OK`를 확인했다. C1~C10은 실제 원천 품질 이슈로 `severity_max=ERROR`이며 C2/C4/C6/C7 data-quality CSV 8개와 DB size snapshot을 남겼다. 상세: `docs/t027-fullload-plan.md` (2026-05-29)
- [x] T-055 N150/Odroid 운영 환경 비교 준비. 장비 도착 전 수행 가능한 범위를 완료했다. `scripts/capture_deployment_envelope.py`가 시스템 envelope를 `system-envelope.json/md`로 캡처하고, `fio`/`sysbench`는 `--run-probes`일 때만 실행한다. T-047 SQL/REST benchmark, MV refresh benchmark, T-027 full-load script를 장비별 같은 산출물 구조로 실행하는 runbook을 `docs/t055-deployment-n150-odroid.md`에 고정했다. 실제 하드웨어 실측은 T-063으로 보류한다. (2026-05-29)
- [x] T-054 한국 IP 외부 접근 차단(GeoIP gate). FastAPI middleware가 내부/loopback은 허용하고 외부 공용 IP는 GeoIP country `KR`만 통과시키게 했다. 기본 `strict` 모드에서는 GeoIP DB가 없으면 공용 IP를 `E0403/403`으로 차단하며, allow/deny CIDR, trusted proxy `X-Forwarded-For`, `geoip.denied` audit, `ktgctl geoip check`를 지원한다. 상세: `docs/t054-korea-only-geoip.md`, ADR-037 (2026-05-29)
- [x] T-059 CLI/Job 동시 실행 보호 표준화. `infra.concurrency`의 PostgreSQL session advisory lock helper를 추가하고, 주요 CLI 운영 명령과 FastAPI `JobQueue` handler가 같은 lock key를 공유하도록 했다. 같은 key를 다른 connection이 이미 잡고 있으면 `E0409/HTTP 409` 또는 CLI exit code 2로 fail-fast한다. Docker PostgreSQL smoke에서 `MV_REFRESH` 중복 lock 차단을 확인했다. 상세: `docs/t059-concurrent-job-protection.md` (2026-05-29)
- [x] PR #69~#82 post-merge 리뷰 audit/fixup. PR #69부터 최신 PR #82까지 formal review와 review thread를 재확인했고 unresolved thread 0건을 기록했다. PR #81 리뷰에서 발견된 `maintenance_window.authorize` audit의 `actor_type="job"` CHECK 위반을 `system`으로 고쳤다. 상세: `docs/postmerge-review-fixups-pr69-pr82.md` (2026-05-29)
- [x] PR #69~#80 post-merge 리뷰 audit/fixup. PR #69부터 최신 PR #80까지 formal review와 review thread를 재확인했고 unresolved thread 0건을 기록했다. 수동 table stats capture lock 충돌은 `409 E0409`로 구분하고, `replace_current` restore의 maintenance window 인가 통과는 `maintenance_window.authorize` audit event로 남긴다. 상세: `docs/postmerge-review-fixups-pr69-pr80.md` (2026-05-29)
- [x] T-058 적재 완료 DB restore hot-swap plan/preflight. 같은 cluster 안 `ALTER DATABASE ... RENAME` 패턴을 1차 운영 절차로 고정하고, `/v1/admin/restores/hot-swap-plan`과 `ktgctl serving hot-swap-plan`이 current DB, restore DB, previous alias, typed confirmation, rollback confirmation, blockers, SQL/steps를 산출하도록 했다. 실제 rename 실행은 metadata 위치와 worker별 engine refresh를 더 검증한 뒤 후속 실행 표면으로 분리한다. 상세: `docs/t058-restore-hot-swap.md`, ADR-036 (2026-05-29)
- [x] T-050 운영 hardening 7차 실제 PostgreSQL 제약 통합 테스트. `KTG_TEST_PG_DSN` 기반 선택형 테스트를 추가해 실제 PostgreSQL에서 `ops.audit_events.job_id` FK, append-only trigger, `ops.serving_releases` active partial unique index, `ops.table_stats_snapshots.snapshot_id` FK를 검증한다. Docker PostgreSQL 별도 DB `kor_travel_geo_t050_ops_constraints`에서 `1 passed`를 확인했다. 상세: `docs/t050-ops-hardening.md` (2026-05-29)
- [x] T-050 운영 hardening 6차 destructive confirmation flow. `db_restore`의 `replace_current` 위험 경로가 `target_dsn` 없이 현재 DB명 target, `RESTORE <현재 DB 이름>` typed confirmation, active `restore` maintenance window를 모두 만족해야 실행되도록 보강했다. 상세: `docs/t050-ops-hardening.md` (2026-05-29)
- [x] T-050 운영 hardening 5차 table stats 주기 capture. API lifespan에서 opt-in scheduler를 띄워 `ops.table_stats_snapshots`를 주기 수집할 수 있게 했다. 기본 interval은 0으로 비활성화하고, 수동/주기 capture에서 `snapshot_id`를 생략하면 현재 active serving release snapshot에 자동 연결한다. 연결 방식은 `stats.snapshot_link`에 남긴다. 상세: `docs/t050-ops-hardening.md` (2026-05-29)
- [x] PR #69~#75 post-merge 리뷰 audit/fixup. PR #69부터 최신 PR #75까지 formal review와 review thread를 재확인했고 unresolved thread 0건을 기록했다. `maplibre-vworld` lockfile URL, consistency sample 조회 최적화, backup progress sample 캐시, release hook gate/count 보강, 운영 runbook 문서화를 반영했다. 상세: `docs/postmerge-review-fixups-pr69-pr75.md` (2026-05-29)
- [x] T-050 운영 hardening 4차 snapshot/release hook. full-load/MV/restore 완료 지점을 `ops.dataset_snapshots`/`ops.serving_releases`와 자동 연결했다. `mv_refresh` 성공 시 active release를 만들고 restore 성공 시 pending restore release 후보를 기록한다. PR #75 리뷰 후속으로 load-batch ERROR gate를 swap 이전으로 옮기고 `mv_hash` 계산에서 대형 MV count를 재사용하도록 보강했다. 상세: `docs/t050-ops-hardening.md` (2026-05-29)
- [x] T-061 Q3 fuzzy slim text-search 구조. `mv_geocode_target`에서 재생성 가능한 read-only helper MV `mv_geocode_text_search`를 추가하고, Q3 fuzzy geocode와 Q4 broad search fallback 후보 추출에 사용하도록 분리했다. Q4 exact preflight는 기존 target exact index를 유지했다. T-057 corpus 기준 Q3 fuzzy c64 p95는 359.25ms → 227.57ms, `sig_cd` hint는 193.36ms → 182.27ms, wide는 255.36ms → 200.69ms로 개선됐다. helper는 6,416,637행, heap 854MiB, index 1,572MiB, total 2,426MiB이며 helper 포함 shadow swap은 497.54초로 측정했다. 상세: `docs/t061-slim-text-search.md` (2026-05-28)
- [x] T-053 Admin Web UI C1~C10 상세 분석/수동 판정 콘솔. `docs/t053-admin-ui-ops-statistics.md`에 사용자 재확인 의도를 기준 설명·지도 overlay·테이블 비교·수동 승인/거절/보류·v1/v2 API 경계로 구체화했다. `ops.consistency_case_samples` row-per-sample 저장, case definitions/sample list/summary/single·bulk decision/recheck/CSV admin API, OpenAPI/프론트 타입, `/admin/consistency/[report_id]` 상세 UI(TanStack Query, TanStack Table, Zustand, maplibre-vworld-js wrapper 기반 지도)를 추가했다. 상세: `docs/t053-admin-ui-ops-statistics.md` (2026-05-28)
- [x] T-052 외부 API 스타일 비교 + API v1(vworld 호환)/v2(신규) 분리 + AI-friendly 문서화. 사용자 재확인에 따라 v2는 Kakao/Naver/Google/VWorld 직접 wrapper가 아니라 각 API 스타일의 장점을 참고한 자체 candidate schema로 정리했다. `dto/v2.py`, `/v2/geocode|reverse|search`, `AsyncAddressClient.*_v2`, `docs/api-reference/`, OpenAPI와 frontend 생성 타입을 추가했다. 상세: `docs/t052-api-providers-v1-v2.md` (2026-05-28)
- [x] T-056 `python-legacy-address-base` Address 코드 helper 정리. 실제 `~/dev/python-legacy-address-base`는 Git checkout이 아니고 license가 `GPL-3.0-or-later`라 원본 코드를 복사하지 않았다. 대신 `core/address/codes.py`에 시군구/법정동/도로명관리번호/도로명주소관리번호 helper를 공개 주소 코드 규칙 기반 독립 구현으로 두고, Juso fallback 좌표 API 파라미터 정규화에 연결했다. 상세: `docs/t056-legacy-address-base-address-merge.md`, ADR-035 (2026-05-28)
- [x] T-044 `maplibre-vworld-js` 0.1.0 기준 코드/API 재확인과 문서-only 보강. GitHub tag `v0.1.0` commit `8559bf4f8d5a32011a51669552bb7e1aedd42cfb`를 기준으로 package manifest, `src/index.ts`, `VWorldMap`, marker/layer primitive, helper export를 확인했다. npm registry에는 아직 `maplibre-vworld@0.1.0`이 없어 실제 dependency 갱신은 하지 않았고, `maplibre-vworld-js` upstream 코드는 직접 수정하지 않았다. T-044 결론과 후속 wrapper 구현 메모는 문서에만 남겼다. 상세: `docs/t044-maplibre-vworld-010-review.md` (2026-05-28)
- [x] T-062 PR #53~#64 post-merge 리뷰 코멘트 audit/fixup. T-057 종료 직후 PR #53부터 #64까지 conversation/review/inline/thread 표면과 GraphQL `reviewThreads`를 재확인했고 unresolved thread 0건을 기록했다. PR #53의 exact preflight 정규화/benchmark blind spot, PR #55의 `pg_stat_statements` schema prefix, PR #59의 Pydantic validation string matching, PR #62의 c64 중심 비교 설명, PR #63의 checksum 단계와 `tar.zst` 해석을 코드/문서로 보강했다. 상세: `docs/postmerge-review-fixups-pr53-pr64.md` (2026-05-28)
- [x] T-057 행정구역 hint 기반 검색 가속 1차 구현과 실측. `RegionHint(sig_cd,bjd_cd)` DTO를 추가하고 `AsyncAddressClient.geocode/search/reverse_geocode`, `/v1/address/geocode`, `/v1/address/search`, `/v1/address/reverse`에 선택 hint를 연결했다. 현재 `mv_geocode_target`에는 물리 `sig_cd`가 없어 `bjd_cd` prefix filter로 적용했고, 응답 구조는 vworld 호환 그대로 유지했다. SQL standard run은 900 case/8,100 measurement/error 0이었고 Q3 fuzzy c64 p95는 307.45ms → 267.99ms로 개선됐다. REST smoke는 320 case/1,920 measurement/error 0이었고 Q1/Q3/Q4/Q6은 개선, Q2/Q5는 악화 구간이 있어 반복 측정 후보로 남겼다. Q3 전용 slim text-search 구조는 T-061로 분리한다. 상세: `docs/t057-region-hint-search.md` (2026-05-28)
- [x] T-047 backup archive 압축 측정. `apt download zstd`로 로컬 `zstd v1.5.5`를 확보한 뒤 T-047 operational impact의 `pg_dump -Fd` directory를 실제 `tar.zst`로 포장했다. archive wall time은 33.31초, RSS max는 112,768KiB, archive 크기는 4,308,457,630 bytes였고 SHA256은 `94f404bdf9a4a3956009f961f966e7bca3b90f42eecfc083e83add7b1ea87883`였다. dump 내부 `.dat.gz`가 이미 압축되어 있어 크기 감소는 작았다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-28)
- [x] T-047 REST admission candidate 반복 측정. 기본 profile, `w2/p8/a8`, `w4/p4/a4`를 같은 REST corpus로 `iterations=3` 재측정했다. 세 run 모두 16,000 measurement/error 0이었다. `w2/p8/a8`은 Q1/Q4 p95와 Q1~Q4 p99가 더 안정적이었고, `w4/p4/a4`는 Q7/Q8/Q11에서 강했다. Q3 fuzzy는 p95 기준 기본 profile이 가장 낮아 worker/pool/admission만으로 확정 개선했다고 보지 않고, T-057 region hint 또는 text-search slim MV 후속으로 넘긴다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-28)
- [x] T-047 REST worker/pool/admission grid. `/v1/address/*` 전용 optional admission control을 추가하고 같은 REST corpus로 `w1/p16/a16`, `w2/p8/a8`, `w4/p4/a4`를 측정했다. 세 run 모두 error 0이었다. `w4/p4/a4`는 Q4 search c64 p95를 753.25ms → 435.63ms, Q3 fuzzy를 810.53ms → 550.35ms로 낮췄지만 Q5 reverse/Q8 no-result는 악화됐다. 기본값은 admission 비활성으로 유지하고, 권장 profile은 `w4/p4/a4`와 `w2/p8/a8`의 `iterations=3` 재측정 후 확정한다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-28)
- [x] T-047 REST API pool64 비교. 같은 1,000 REST case/8,000 measurement corpus를 uvicorn 단일 process에서 DB pool 64(`max_overflow=0`)로 재측정했다. error 0이었고 Q3 fuzzy c64 p95는 810.53ms → 557.25ms로 개선됐지만, Q1/Q2/Q4/Q5/Q7/Q8은 대부분 악화되어 운영 기본 pool을 단순히 64로 올리지 않기로 했다. 다음 실험은 `workers × pool size × admission limit` grid와 T-057 region hint 비교로 좁혔다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-28)
- [x] T-047 REST API e2e latency. `scripts/benchmark_api_latency.py`를 추가해 저장 corpus를 `/v1/address/*` 요청으로 변환하고, 1,000 REST case/8,000 measurement를 측정했다. error 0, c1 p95 6.95~16.18ms, c16 p95 43.79~97.13ms, c64 p95 479.65~810.53ms였다. 한국 밖 reverse 좌표가 내부 `pydantic.ValidationError`로 500을 반환하던 문제도 HTTP 400 `E0102`로 보정했다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-28)
- [x] T-047 stress corpus benchmark. 11,000건 corpus SHA `2123e09...`와 88,000 measurement로 기본 pool `c1/c4/c16/c64`를 측정했다. error는 0이었고 c16까지 모든 query군 p95가 34ms 이하였으며, c64 tail은 대부분 checkout 대기였다. Q3 fuzzy c64는 p95 335.01ms 중 checkout p95 304.91ms/execute p95 32.07ms, Q4 search c64는 p95 302.21ms 중 checkout p95 280.41ms/execute p95 27.77ms였다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-28)
- [x] T-047 인덱스 운영 영향 측정. T-047 exact btree index 3개 포함 상태에서 MV refresh/swap, 디스크, 백업 envelope를 측정했다. `CONCURRENTLY` refresh는 T-035 기준 111.64초에서 133.28초로 증가했고, shadow `swap`은 137.15초에서 352.85초로 증가했다. exact index 3개 build phase 합계는 180.35초, exact index total size는 1.43GiB, `pg_dump -Fd --jobs=4` dump directory는 2분 21.60초/4.02GiB였다. `tar.zst` archive 측정은 후속 T-047 backup archive 압축 측정에서 완료했다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-28)
- [x] T-047 active observability run. `pg_stat_statements` preload로 Docker DB를 재시작하고 extension을 활성화한 뒤, 1,100건 corpus SHA `ef460f8...`로 `standard --iterations 3`를 실행했다. 기본 pool c64 tail은 대부분 checkout 대기였고(Q4 p95 330.80ms, checkout p95 307.88ms, execute p95 28.09ms), pool64 c64에서는 checkout p95가 28.12ms로 줄고 Q4 p95가 162.50ms로 낮아졌다. Alembic 33자 revision ID 문제도 32자 이하로 정정했다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-28)
- [x] T-047 관측성 benchmark 보강. `scripts/benchmark_query_performance.py` artifact schema 2에 measurement별 `checkout_ms`/`execute_ms`, summary별 `p95_checkout_ms`/`p95_execute_ms`를 추가해 pool wait와 SQL execution을 분리했다. `pg_stat_statements` before/after/delta artifact, reset 옵션, Docker preload 설정, fresh schema/Alembic extension 경로를 추가했다. 기존 T-027 DB smoke에서는 extension 미설치 상태를 artifact에 기록했고 11개 query군 error 0을 확인했다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-28)
- [x] T-060 PR #51/#52 리뷰 코멘트 audit/fixup. PR #51/#52의 conversation/review/inline/thread 표면을 다시 확인했고, 두 PR 모두 conversation post-merge review 1건씩, review 0건, review thread 0건이었다. Q4 search split은 PR #53에서 exact preflight로 반영됐고, 남은 항목은 `pg_stat_statements`, T-047 index 운영 영향, stress corpus, pool wait/DB execution 분리, SQL 상수 public module 추출, Q3 fuzzy/T-057 region hint 후속으로 정리했다. 상세: `docs/postmerge-review-fixups-pr51-pr52.md` (2026-05-28)
- [x] T-047 Q4 search exact preflight 튜닝. `rn_nrm`/`buld_nm_nrm` exact btree index와 저장소 2단계 search 경로를 추가했다. Q4 100건 standard corpus는 모두 exact preflight로 처리됐고, `Q4-search-038` plan execution은 broad trigram 42.39ms에서 exact 0.56ms로 감소했다. Q4 p95는 default pool c1/c4/c16에서 62.12/70.62/116.06ms → 12.23/22.39/52.27ms, pool64 c64에서 481.22ms → 295.85ms로 개선됐다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-28)
- [x] T-047 standard corpus와 pool 비교. PR #51에서 추가한 harness로 1,100건 corpus(`cases_per_group=100`)를 만들고 동시성 1/4/16/64를 측정했다. 기본 pool에서는 동시성 16까지 모든 query군 p95가 ADR-031 1차 목표 안에 들었고, 동시성 64 tail은 pool 대기와 DB 경합이 섞였다. `--pool-size`/`--max-overflow` 옵션을 추가해 같은 corpus를 pool 64로 재측정했고, Q2/Q8은 개선됐지만 Q3/Q4는 악화되어 query split/text-search slim MV가 다음 튜닝 후보로 남았다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-28)
- [x] T-047 1차 query benchmark harness와 지번 exact 튜닝. `scripts/benchmark_query_performance.py`와 단위 테스트를 추가하고, T-027 최종 클린 DB(`mv_geocode_target=6,416,637`, `tl_sppn_makarea=24,204`)에서 smoke/concurrency benchmark를 실행했다. `idx_mv_jibun_name_exact`를 추가해 Q2 지번 exact 단일 샘플 client latency를 2830.59ms에서 5.58ms로 줄였고, post-index small concurrency run에서 단일 동시성 모든 query군 p95가 ADR-031 1차 목표 안에 들어왔다. 상세: `docs/t047-query-performance-tuning.md` (2026-05-27)
- [x] T-051 에이전트별 고정 worktree와 CodeGraph 운용 문서화. 초기 `geo-*` worktree 정책을 ADR-034와 개발 문서에 추가했다. 이 정책은 T-072/ADR-041에서 NTFS `kor-travel-geo-*` worktree 정책으로 대체됐다. (2026-05-27)
- [x] PR #34~#47 post-merge 리뷰 audit/fixup. GitHub conversation comment, review body, inline review thread, GraphQL `reviewThreads`를 모두 확인했고 unresolved current thread 0개를 기록했다. PR #35의 `source_set` typed schema 약화는 nested JSON 보존으로 보강했고, PR #38의 `maplibre-vworld-js` 최신 SHA 확인·갱신과 WSL frontend 검증 helper를 반영했으며, PR #43의 `ops.audit_events.job_id` FK는 `ON DELETE NO ACTION`으로 바꿨다. 상세: `docs/postmerge-review-fixups-pr34-latest.md` (2026-05-27)
- [x] T-042 `TL_SPPN_MAKAREA` 국가지점번호 보조 데이터 적재/조회 구현. `tl_sppn_makarea` DDL/Alembic, GDAL Python binding 기반 loader, `ktgctl load sppn-makarea`, API job kind `sppn_makarea_load`, source set optional child, 국가지점번호 parser/formatter, geocode/reverse `x_extension.sppn_makarea` 보조 조회를 추가했다. Docker PostGIS `kor_travel_geo_t042_sppn`에서 세종 `구역의 도형` 실제 ZIP을 적재해 146행/146 distinct key/전체 valid MultiPolygon을 확인했고, `금이산` polygon 내부 점을 `다바 7363 4856`으로 formatter → geocode/reverse 조회까지 검증했다. 상세: `docs/t042-sppn-makarea.md` (2026-05-27)
- [x] T-046 적재 완료 DB 백업/복원 및 UI. `pg_dump -Fd --jobs` directory dump를 `tar.zst` archive로 묶고 `ops.artifacts`에 `db_backup` metadata를 저장하는 `infra.backup` 실행 경로를 추가했다. `/v1/admin/backups`, `/v1/admin/restores`, `/v1/admin/jobs/{job_id}/events`, `ktgctl backup/restore`, `/admin/backups` UI, HMAC download token, allowlist path/symlink/callback host 검증, command password redaction, checksum/manifest 검증 테스트를 포함한다. Docker PostGIS에서 대구광역시 부분 DB를 실제 적재해 `t046_daegu_backup.tar.zst` 83MiB 백업, 새 DB 복원, row count 일치, geocode/reverse smoke `OK`를 확인했다. 상세: `docs/t046-db-backup-restore.md` (2026-05-27)
- [x] T-045 원천 자료 기준월 선택과 대용량 업로드/적재 UX. `SourceCandidate`/`SourceSetDiscovery`/`SourceSetPlan`/`UploadSetStatus`/`UploadFileStatus` DTO, `infra.source_set` 탐지·계획 helper, JSON manifest 기반 upload set 저장소, `/v1/admin/uploads/*`와 `/v1/admin/load-sources/*` REST API, `AsyncAddressClient` 메서드, `ktgctl load full-set`, `/admin/load` 다중 파일/DND 업로드와 기준월 확인 modal을 구현했다. 혼합 기준월은 정확한 token 없이는 plan 생성이 실패하고, batch payload에는 source set 감사 필드와 명시 child job 목록이 남는다. 상세: `docs/t045-source-set-load-ux.md` (2026-05-27)
- [x] T-049 운영 메타데이터·감사·릴리스 스키마 구현. `ops` 스키마와 `ops.audit_events`, `ops.dataset_snapshots`, `ops.serving_releases`, `ops.artifacts`, `ops.maintenance_windows`, `ops.table_stats_snapshots` DDL/Alembic migration을 추가했다. `/v1/admin/ops/*` API, `AsyncAddressClient` 메서드, `/admin/ops` 관리 UI, audit redaction/hash helper, active release partial unique index, append-only audit trigger, table stats snapshot capture, typed maintenance confirmation hash를 함께 구현했다. secret/DSN/token/address 원문이 audit payload에 남지 않는 테스트를 추가했다. 상세: `docs/t049-ops-metadata-schema.md` (2026-05-27)
- [x] T-043 PR #23~#41 리뷰 코멘트 일괄 audit/fixup. GitHub conversation comment, formal review body, flat inline comment, GraphQL reviewThreads를 모두 재확인했고 unresolved thread가 0개임을 기록했다. PR #23/#24/#25/#28/#32/#33의 소규모 반영 가능 항목을 코드/문서에 직접 반영하고, 나머지는 T-049/T-045/T-042/T-027/T-047/T-044 후속으로 이관했다. 상세: `docs/postmerge-review-fixups-pr23-latest.md` (2026-05-27)
- [x] T-048 `maplibre-vworld-js` 최신 동기화와 책임 경계 재정의. `kor-travel-geo-ui`의 `maplibre-vworld` dependency를 upstream `main` 최신 commit `7947b2e170ddb36ab28a7a9034dd4dbf8f18370b`로 갱신하고, ADR-032로 "공용 VWorld/MapLibre 기능은 upstream, 지오코딩/역지오코딩/관리 UI 특화 기능은 이 저장소" 원칙을 확정했다. 최신성 확인 명령과 frontend 검증 기준을 문서화했다. (2026-05-27)
- [x] T-037 geometry 포함 SHP 대형 레이어 적재 튜닝. `TL_SPBD_BULD`를 운영 테이블 직접 append 대신 projection staging table + 운영 테이블 insert-select 경로로 분기했다. 세종 단일 레이어는 기존 38.36초에서 18.59초로 줄었고, 경기도 1,649,975행은 40분 17.15초에 성공했다. 상세: `docs/t037-shp-geometry-tuning.md` (2026-05-26)
- [x] T-041 상세주소 동 도형/구역 추가 레이어 검토. 세종/경남 실제 `건물군 내 상세주소 동 도형`이 전자지도 `TL_SPBD_BULD`의 부분집합임을 확인했고, `구역의 도형` 중 기존 행정/기초구역 5개 레이어는 전자지도와 key 기준 완전 중복임을 확인했다. `TL_SCCO_GEMD`는 별도 overlay/분석 후보로 보류하고, `TL_SPPN_MAKAREA`는 ADR-027에서 국가지점번호 보조 데이터 후보로 승격했다. 상세: `docs/t041-detail-zone-shape-layers.md` (2026-05-26)
- [x] T-040 `도로명주소 건물 도형` bundle 비교. 세종/경남 실제 `TL_SGCO_RNADR_MST`, `TL_SPBD_ENTRC`, `TL_SPOT_CNTC`를 전자지도 `TL_SPBD_BULD`/`TL_SPBD_ENTRC`와 natural key로 비교했고, 단순 중복이 아니므로 현행 serving table에는 섞지 않기로 ADR-025에서 결정했다. 비교 helper/script와 실제 파일 테스트를 추가했다. 상세: `docs/t040-building-shape-bundle.md` (2026-05-26)
- [x] T-039 `도로명주소 출입구 정보` direct entrance loader 구현. `RNENTDATA_2605_*.txt`를 `tl_roadaddr_entrc`에 별도 적재한다. T-027 최종 클린 적재 재검증 결과 기준월이 다른 direct 출입구를 바로 serving 좌표로 승격하면 C4/C6/C7이 증가하므로, MV 대표 좌표는 `tl_locsum_entrc` → same-month `tl_roadaddr_entrc` → `tl_navi_buld_centroid` 순서로 선택한다. 실제 전국 17개 ZIP 6,418,169행 구조, 세종 유효 좌표 27,779행, Docker DB 샘플 적재와 same-month gate를 검증했다. 상세: `docs/t039-roadaddr-entrance-loader.md` (2026-05-26, 2026-05-27 보정)
- [x] T-038 `tl_juso_parcel_link` DDL/로더 구현. `jibun_rnaddrkor_*` full snapshot과 daily `TH_SGCO_RNADR_LNBR.TXT` delta를 별도 1:N 테이블에 적재하고, CLI/API job kind/full-load batch/UI 기본 payload를 연결했다. 실제 Docker DB에서 `jibun_rnaddrkor_seoul.txt`와 `20260401_dailyjusukrdata.zip` LNBR 샘플 적재를 검증했다. 상세: `docs/t038-parcel-link-loader.md` (2026-05-26)
- [x] T-030 상세주소 동 도형/별도 건물 도형 로더 검토. 세종 실제 ZIP 기준으로 `건물군 내 상세주소 동 도형`, `구역의 도형`, `도로명주소 건물 도형`, `도로명주소 출입구 정보`의 layer/geometry/text 구조를 확인하고, 기본 full-load에 즉시 섞지 않고 T-039~T-041로 분리하기로 ADR-023에서 확정했다. 상세: `docs/t030-extra-shape-sources.md` (2026-05-26)
- [x] T-029 `jibun_rnaddrkor_*` 활용 여부 결정. 실제 전국 `jibun_rnaddrkor_*` 1,769,370행과 daily `LNBR` 구조를 확인하고, `tl_juso_text.pnu`에 덮어쓰지 않고 후속 `tl_juso_parcel_link` 1:N 테이블로 분리하기로 ADR-022에서 확정했다. 상세: `docs/t029-jibun-rnaddrkor-decision.md` (2026-05-26)
- [x] T-028 일변동 ZIP 로더. `data/juso/daily/*.zip`의 `TH_SGCO_RNADR_MST.TXT`를 `tl_juso_text`에 UPSERT/DELETE로 적용하고, `TH_SGCO_RNADR_LNBR.TXT`는 T-038 전까지 manifest에 미지원 행 수로 기록한다. 실제 `20260401_dailyjusukrdata.zip` MST 422행과 `20260404` `No Data` member를 검증했다. 상세: `docs/t028-daily-juso-delta.md` (2026-05-26)
- [x] PR #20~#22 post-merge 리뷰 반영. PR #22 → PR #21 → PR #20 순서로 리뷰 코멘트를 확인하고, T-035 benchmark metadata/public helper/lock timeout, T-034 DBF COPY 오류 문맥/row dataclass/deleted record test, T-033 full-load phase timer/C10 설명을 보강했다. 상세: `docs/postmerge-review-fixups-pr20-pr22.md` (2026-05-26)
- [x] T-036 `maplibre-vworld-js` main 동기화. `kor-travel-geo-ui`의 `maplibre-vworld` dependency를 upstream main commit `c91c9f304669ce3f5fc4915f21186b23731d5816`로 갱신하고, `redactVWorldUrl()` helper를 기존 내부 이름 `redactVWorldTileUrl`로 alias해 디버그 UI 계약을 유지했다. 최신 upstream redaction 표기 `***`를 테스트로 고정하고 frontend `npm ci`/lint/type/test/build를 검증했다. 상세: `docs/t036-maplibre-vworld-sync.md` (2026-05-26)
- [x] T-035 MV refresh/swap 벤치마크. `scripts/benchmark_mv_refresh.py`로 전국 DB `kor_travel_geo_t033`에서 `CONCURRENTLY`와 shadow swap을 비교했다. `CONCURRENTLY`는 1분 49.64초, shadow swap은 2분 16.28초였고, swap rename/index rename 구간은 약 0.016초였다. `shadow_swap_mv()`는 `ANALYZE`를 별도 transaction으로 분리했다. 상세: `docs/t035-mv-refresh-benchmark.md` (2026-05-26)
- [x] T-034 SHP GDAL append 병목 튜닝. `TL_SPRD_INTRVL`은 geometry 없는 DBF 속성 레이어라 GDAL append 대신 직접 DBF scan + `psycopg COPY` 경로로 분기했다. 세종 단일 레이어는 36.12초에서 1.59초로 줄었고, 경기도 2,677,715행은 새 경로에서 15.88초에 적재됐다. 상세: `docs/t034-shp-append-tuning.md` (2026-05-26)
- [x] T-033 전국 full-load 성능 재검증. PR #20에서 `kor_travel_geo_t033` 빈 DB에 실제 전국 데이터 full-load를 수행했다. 전체 4시간 8분 2초, SHP 153 layers, MV 6,416,637행, smoke test 통과, data-quality CSV 8개 export를 기록했다. 상세: `docs/t033-full-load-revalidation.md` (2026-05-26)
- [x] T-032 full-load/정합성 성능 튜닝. PR #19에서 C4/C6/C7 중복 공간 스캔 제거, 정합성 CTE materialization, SHP 다중 시도 적재 마지막 1회 `ANALYZE`, postload timeout 보강, 세종특별시·경상남도 축소 검증 1회를 완료했다. 전국 full test와 반복 trial은 T-033~T-035로 분리했다. 상세: `docs/t032-performance-tuning.md` (2026-05-25)
- [x] T-031 T-027 데이터 품질 후속 분석. PR #17에서 C2/C4/C6/C7 CSV export CLI, SHP `source_file` 추적성, 실제 Docker DB 1차 실행 결과를 정리했다. 상세: `docs/t027-data-quality-followup.md` (2026-05-25)
- [x] T-026 디버그 UI `/admin/consistency` 페이지 구현. `GET /v1/admin/consistency`, `GET /v1/admin/consistency/{report_id}`, `POST /v1/admin/consistency/run`을 사용해 C1~C10 리포트 목록, 케이스별 severity/count, 원본 JSON을 확인한다 (2026-05-23)
- [x] T-025 `prometheus-client` 메트릭 구현. 외부 API 호출 success/failure counter, `geo_cache` entries/hits/expired gauge, `load_jobs` kind/state gauge를 `/metrics`에서 노출한다. 라이브러리 단독 설치 환경에서는 no-op fallback으로 import 실패를 피한다 (2026-05-23)
- [x] T-024 `pre-commit`, `import-linter`, `mypy` CI 정착. 루트 `.pre-commit-config.yaml`과 `.github/workflows/ci.yml`을 추가하고 backend/frontend lint·type-check·test·build·OpenAPI type drift 검사를 묶었다 (2026-05-23)
- [x] T-023 프론트엔드 `/admin/load`, `/admin/tables`, `/admin/cache`, `/admin/logs` 구현. full-load batch payload 입력, raw ZIP 업로드, MV refresh enqueue, 테이블 통계, 캐시 메트릭, `load_jobs.log_tail` 조회를 제공한다 (2026-05-23)
- [x] T-022 프론트엔드 `/debug/geocode`, `/debug/reverse`, `/debug/normalize`, `/debug/explain` 구현. 지오코딩/역지오코딩/정규화/EXPLAIN 요청을 Next.js Route Handler 프록시로 백엔드에 전달하고 JSON 응답을 즉시 확인한다 (2026-05-23)
- [x] T-021 프론트엔드 패키지 `kor-travel-geo-ui` 부트스트랩. Next.js 16 + React 18 + Tailwind + TanStack Query + MapLibre GL JS + VWorld WMTS + OpenAPI 타입 생성 스크립트 기반의 별도 Node.js 패키지를 추가했다. `digitie/maplibre-vworld-js`는 범용 VWorld/MapLibre 문제 발생 시 적극 수정 대상으로 둔다 (2026-05-23, 2026-05-25 갱신)
- [x] T-020 OpenAPI export 스크립트 + `openapi.json` 생성 + GitHub Actions drift 검사 (`scripts/export_openapi.py`, `.github/workflows/openapi.yml`) 구현 (2026-05-23)
- [x] T-019 외부 API 폴백(`fallback="api"`) — vworld 주소 좌표 API와 juso 검색+좌표 API 어댑터 구현. 로컬 `NOT_FOUND`일 때만 호출하고 `x_extension.source`에 공급자 출처 기록 (2026-05-23)
- [x] T-018 CLI 운영 명령 구현 — `load all-sidos`, `load shp`, `load shp-all`, `load pobox`, `load bulk`, `load epost --kind=full`, `refresh mv --swap`, `validate consistency --cases/--scope` (2026-05-23)
- [x] PR #10 리뷰 fixup: ADR-017 batch DAG(`load_batch_id`, `parent_job_id`, `full_load_batch`), C1~C10 정합성 검증, PNU `mntn_yn IS NULL` guard, reverse `both`, 인코딩 fallback, `load_jobs.log_tail` 갱신 경로, ADR-018 문서화 (2026-05-23)
- [x] T-017 `loaders/pobox_loader.py`, `loaders/bulk_loader.py` (epost 보조 우편번호 COPY 로더) 구현 (2026-05-23)
- [x] T-016 reverse / search / zipcode / pobox 코어와 raw SQL repo, REST 라우터 구현 (2026-05-23)
- [x] T-015 `api/_jobs.py` 작업 큐(`load_jobs` 영속화, startup running 복구, advisory lock + `FOR UPDATE SKIP LOCKED`)와 `/v1/admin/loads`, `/v1/admin/consistency/*` 표면 구현 (2026-05-23)
- [x] T-014 `loaders/shp/delta_loader.py` (settings/DB 기반 `MVM_RES_CD` action 매핑을 받는 polygon delta merge helper) 구현 (2026-05-23)
- [x] T-013d `loaders/shp/polygons_loader.py` (GDAL `VectorTranslate`, ADR-012 보조 SHP 9종 load plan) 구현 + 실제 강원 SHP load plan 검증 (2026-05-23)
- [x] T-013c `loaders/text/navi_loader.py` (내비게이션용DB centroid + 진입점 parser/COPY) 구현 + 실제 서울 파일 검증 (2026-05-23)
- [x] T-013b `loaders/text/locsum_loader.py` (위치정보요약DB 출입구 좌표 parser/COPY, `bd_mgt_sn` 후해소 구조) 구현 + 실제 ZIP member 검증 (2026-05-23)
- [x] T-013a `loaders/text/juso_hangul_loader.py` (도로명주소 한글_전체분 parser/COPY, NULL-safe PNU 검증) 구현 + 실제 서울 파일 검증 (2026-05-23)
- [x] T-012 `api/app.py`, geocode/reverse/search/zipcode/pobox/admin 라우터 구현 (2026-05-23)
- [x] T-011 `AsyncAddressClient` 실제 engine/repo 연결, geocode/reverse/search/zipcode/pobox/load/consistency 메서드 구현 (2026-05-23)
- [x] T-010 `core/geocoder.py` 구현 + Fake repo 단위 테스트 (2026-05-23)
- [x] T-009 `core/normalize.py` 주소 정규화 순수 함수 구현 (2026-05-23)
- [x] T-008 `infra/geocode_repo.py` 및 reverse/search/zip/pobox/admin raw SQL repo 구현 (2026-05-23)
- [x] T-007 `mv_geocode_target` MV 정의(`pt_5179`/`pt_4326`/`pt_source`, partial GiST index) + 실제 PostgreSQL 샘플 load/MV 생성 검증 (2026-05-23)
- [x] T-006 텍스트 4 + SHP polygon/폴리라인 9 + 보조 2 + 메타 5 = 20개 테이블 DDL + Alembic 0001 작성. `tl_juso_text.pnu` NULL-safe generated column, `load_jobs`, `load_consistency_reports` 포함 (2026-05-23)
- [x] T-005 `infra/engine.py` async engine factory 작성 (`Settings.normalize_pg_dsn` 신뢰, `x_extension` search_path, statement timeout) (2026-05-23)
- [x] T-004 `dto/geocode.py`, `dto/reverse.py`, `dto/search.py`, `dto/zipcode.py`, `dto/pobox.py`, `dto/admin.py` 작성 (2026-05-23)
- [x] T-003 `dto/common.py`, `dto/address.py` 작성 + 단위 테스트 (2026-05-22)
- [x] T-002 `Settings` (pydantic-settings) + `.env.example` 작성 (2026-05-22)
- [x] T-001 `pyproject.toml` 작성 (`kor-travel-geo` 패키지, optional extras `api`/`loaders`/`dev`) (2026-05-22)

## 사양 참조
- 백엔드 세부: `docs/backend-package.md`
- 프론트엔드 세부: `docs/frontend-package.md`
- 데이터 모델: `docs/data-model.md`
- 외부 API: `docs/external-apis.md`
