# 디자인 시스템 — kor-travel-geo-ui

`kor-travel-geo-ui`의 모든 운영 화면에 적용하는 고정 디자인 시스템이다. 화면마다 다른
테마를 적용하지 않으며, 업무 흐름과 데이터의 우선순위가 같은 방식으로 읽히게 한다.

## 장르와 구조

- 장르: modern-minimal
- 앱 화면 구조: Workbench. 좌측 레일은 작업 영역을 고정하고, 본문은 입력·결과·지도·표를
  넓은 작업 캔버스 안에 배치한다.
- 내비게이션: 영구 좌측 레일(작은 화면에서는 접근 가능한 drawer).
- 풋터: 별도 사이트 풋터 대신 좌측 레일의 로그아웃 제어만 둔다. 운영 도구에 불필요한
  마케팅성 푸터나 링크 모음은 추가하지 않는다.

## 색상

청록 계열의 기존 제품 인지를 유지한다. 색상은 `app/globals.css`의 이름 있는 OKLCH 토큰만
사용한다.

- 바탕: 차가운 청록 기운의 거의 흰 종이색
- 본문: 남청에 가까운 잉크색
- 강조: 상태·현재 위치·주요 실행에만 쓰는 청록
- 상태: 성공·경고·오류는 색상 외 텍스트와 아이콘으로도 구별한다.

## 서체와 숫자

- 표시: `Pretendard Variable` 우선의 굵은 한글 산세리프
- 본문: `Noto Sans KR` 우선의 읽기용 산세리프
- 코드·JSON: `IBM Plex Mono` 우선의 고정폭 서체
- 표와 시간·용량·카운트에는 `font-variant-numeric: tabular-nums`를 적용한다.

## 간격·모션·상호작용

- 4px 기반의 이름 있는 간격 토큰만 사용한다.
- 카드 대신 얇은 경계, 밀도 차이, 여백으로 작업 영역을 나눈다.
- 모션은 버튼 누름·drawer·modal에만 제한하고 `transform`·`opacity`만 전환한다.
- 모든 인터랙션은 기본·hover·focus-visible·active·disabled 상태를 제공한다. 성공과 오류는
  기존 업무 흐름에서 표시하는 상태 메시지를 그대로 사용한다.

## 화면별 허용 범위

- 디버그: 입력 패널과 결과/지도를 비대칭 작업 그리드로 둔다.
- 관리: 상태 요약은 짧고 조밀하게, 위험 작업은 경고 색과 명시적 문구로 구분한다.
- 로그인·오류: 같은 토큰과 여백을 따르되, 불필요한 앱 내비게이션은 보이지 않는다.

## 일관성 규칙

- 새 색상·서체·반지름은 `app/globals.css` 토큰 블록에 먼저 추가한다.
- 라우트·API 호출·권한·폼 흐름은 시각 개편 범위 밖으로 유지한다.
- `Panel`, `PageHeader`, `DocumentNavLink`가 공통 시각 계약의 중심이며, 개별 화면이 이를
  우회하는 새 카드 체계를 만들지 않는다.
